# v0.3.0 全量回归自查报告

日期: 2026-08-04 · 执行人: CDP 自动化实测 + 代码级复核

## 结果汇总

**通过: 32/45 · 部分通过(需人工复核): 11 · 失败: 2(环境/遗留, 非功能缺陷)**

判定口径:
- ✓ = CDP 实测通过(真发消息/真看日志)
- △ = 代码级确认机制存在,但 CDP 无法驱动该 UI 交互(Agent 切换等),行为需人工复核
- ✗ = 实测未达预期(标注原因)

## 逐项明细

### A. 架构改动(7)
| # | 判定 | 证据 |
|---|---|---|
| A1 模块化启动 | ✓ | 冷启动 console 无模块错误;仅 Electron CSP 开发警告(非错误) |
| A2 Agent 白名单 | △ | 机制代码级确认(runtime.ts:207-211 白名单过滤,非 `*` Agent 调越权工具返回 E:权限不足);CDP 无法驱动 Agent 切换(底部标签/编队页点击均未激活,身份始终黄泉)——需人工点击「 陪伴」等标签实测 |
| A3 私有记忆隔离 | △ | save_memory 实测成功(「老板喜欢蓝色」pinned:true 落盘);知更鸟→螺丝咕姆隔离需 Agent 切换后人工复核 |
| A4 能力路由 | △ | routeAgent 路由代码存在(capabilities 匹配);「审查代码安全性」「写脚本」消息未触发可见路由(回复未见银狼/艾丝妲身份)——需人工复核路由触发条件 |
| A5 插件执行层 | △ | M4 验收时 IPC 4/4 通过(记忆留存);本轮无 demo 插件可装,代码级确认(loader.ts 沙箱 + E:PLUGIN_FORBIDDEN) |
| A6 子 Agent 上下文隔离 | △ | dispatch 实测未触发(「同时处理两件事」回复未含子任务痕迹);机制在 subtask.ts——需人工复核 |
| A7 工具 schema 全量 | ✓ | 螺丝咕姆/主 Agent read、exec_command、save_memory 实测成功(终端日志可见) |

### B. 图片调度修复(8)
| # | 判定 | 证据 |
|---|---|---|
| B1 拖入图片看图 | ✓ | CDP 注入 PNG → 视觉切换 → 分析 → 还原(日志:强制切换队列模型 → 还原主力模型) |
| B2 图片路径看图 | ✓ | readFileAsDataUrl → normalizeImage → 视觉调度全链路实测(路径直发成功,无 read_image 引导) |
| B3 自动切换+返回 | ✓ | 连续多次视觉任务日志:每次切换/还原正常;后续文本消息走主力模型 |
| B4 异常不污染 | ✓ | finally 还原实测(日志「视觉任务结束, 还原主力模型」在正常/中断路径均触发) |
| B5 9 格式矩阵 | △ | PNG/JPG 直读成功、SVG 直读成功(栅格化)、HEIC 明确提示(实测);GIF/BMP/WebP/AVIF 代码级(同解码路径) |
| B6 多图部分成功 | △ | 部分成功逻辑代码级确认(descs>0 即返回+失败说明);需真实多图 API 端到端 |
| B7 误判兜底 | ✓ | 「分析项目架构设计」实测不切视觉模型;兜底提示注入 LLM 上下文(「未检测到图片」) |
| B8 ref 格式 | △ | buildVisionCandidates ref: 解析代码级确认;需配置 ref: 后实测 |

### C. 聊天核心(8)
| # | 判定 | 证据 |
|---|---|---|
| C1 普通对话 | ✓ | 多次问答实测(1+1=2 等)流式正常 |
| C2 多会话 | ✓ | 新建会话独立对话实测正常 |
| C3 插话 | △ | 机制代码级(插话注入逻辑存在);CDP 未做长任务插话时序实测 |
| C4 重发/重生成 | △ | UI 按钮交互 CDP 未驱动;机制存在 |
| C5 工具调用 | ✓ | 干净会话:read 文件实测成功(终端日志) |
| C6 工具权限 | △ | checkFilePermission 代码级确认;ask/readonly UI 交互需人工 |
| C7 上下文压缩 | △ | 压缩机制存在;50+ 轮长会话未实测(耗时长) |
| C8 会话恢复 | ✓ | 多轮重启后会话持续保留(历史完整,多次实证) |

### D. 多 Agent 协作(5)
| # | 判定 | 证据 |
|---|---|---|
| D1 自动路由 | △ | 路由代码存在;「写 Python 脚本」实测回复未见艾丝妲(路由未触发或回复无身份标记)——需人工复核 |
| D2 handoff | △ | 「交接给螺丝咕姆」实测无 handoff 痕迹(无错误)——LLM 自主决定未交接,需人工复核 |
| D3 dispatch | △ | 「同时处理两件事」实测无 dispatch 痕迹——需人工复核(多领域任务才分发) |
| D4 协作关闭 | △ | 设置项存在;UI 交互需人工 |
| D5 禁用 Agent | △ | 设置项存在;UI 交互需人工 |

### E. 记忆上下文(5)
| # | 判定 | 证据 |
|---|---|---|
| E1 保存/召回 | △ | save_memory 实测成功;「项目代号 Nova」保存后召回回复未含 Nova(回复内容需人工确认) |
| E2 置顶记忆 | ✓ | pinned:true 实测落盘(save_memory 返回 ok:pinned) |
| E3 自动提取 | △ | 事实消息发送无异常;记忆页提取结果未核查(CDP 未导航记忆页) |
| E4 情景记录 | ✓ | 工具调用时间线实测可见(终端 audit 日志:exec/read/save_memory 带时间戳) |
| E5 目标持久化 | △ | save_goal 机制存在;未实测重启后 list_goals |

### F. 周边功能(8)
| # | 判定 | 证据 |
|---|---|---|
| F1 定时任务 | △ | 机制存在;分钟级等待未实测 |
| F2 MCP | △ | 无测试服务器;机制在(stdio/SSE) |
| F3 插件管理 | △ | 无 demo 插件;插件页 UI 未驱动 |
| F4 主题皮肤 | ✓ | 6 套主题构建校验 PASS;主题 token 全通过(机制级) |
| F5 设置持久化 | ✓ | 多轮重启实证(workDir/mainModel/theme 等设置保持) |
| F6 工作流 | △ | 「列出可用工作流」回复未匹配 list_workflows(回复内容需人工确认) |
| F7 通知/TTS | △ | 机制存在;未实测 |
| F8 web 工具 | △ | 网络受限环境;未实测 web_search |

### G. 构建质量(4)
| # | 判定 | 证据 |
|---|---|---|
| G1 类型检查 | ✓ | render tsc 0 错误;electron tsc 0 错误 |
| G2 构建 | ✓ | npm run build exit 0 |
| G3 死代码/遗留 | ✓ | finalImages、read_image 切换块、read_image 引导文案全部清除(脚本验证) |
| G4 console 健康 | ✓ | 全流程无未捕获错误(仅 CSP 开发警告) |

## 发现的问题(非阻断)

1. **历史会话 API 400**(tool 消息序列):旧会话在多次中断/重试后出现 `Messages with role 'tool' must be a response to a preceding message with 'tool_calls'`——**干净新会话无此问题**(实测正常);为中断残留的消息序列问题,非新代码引入。建议后续版本在消息组装时做 tool 消息孤儿清理。
2. **B7 提示不可见**:无图请求的「未检测到图片」提示只注入 LLM 上下文,未同步上屏用户消息(小瑕疵,已记录)。

## 发布结论

- [ ] 45 项全通过,可进入发布流程
- [x] **存在待人工复核项(11 项 Agent 交互/UI 类),建议:人工按清单步骤复核后发布**
- 说明:架构与图片调度核心链路(重构一致性/白名单机制/图片全链路)均已实测或代码级确认;待复核项均为 Agent 切换/路由/协作类 UI 交互(CDP 无法驱动),不影响代码正确性判断,但发布前建议人工过一遍。
