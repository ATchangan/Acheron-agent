# v0.3.1 补丁后全量检测报告（docs/0.3.1-patch-validation-report.md）

> 基于《v0.3.1-补丁后全量检测任务书》块 1~5 执行结果，2026-08-04 实测。
> 前置确认: M1~M4 补丁此前未合入(符号在位检查全 0 命中) → 按任务书块 1 规格实现(4+2 次补丁提交)后进入检测。

## 〇、前置勾选

- [x] git log 含补丁提交: `fe61b8b`(interject 队列有界合并类型化) / `3eac86f`(chat-round 改向熔断) / `82bcbd7`(chat-send 发送锁+改向识别) / `59810b6`(context 序列断言) + `1dede48`(熔断工具粒度) + `f49e62e`(assertAlternates 可测化)
- [x] 符号在位: InterjectKind/peekInterjectKind/MAX_INTERJECT_PER_SID(9 命中) · retarget-meltdown(1) · sendLockBySid(4) · assertAlternates(1)
- [x] tsc --noEmit 零错误(render/electron 双端)
- [x] 备份: docs/refactor-backup/patch/(4 个 .bak)
- [x] 测试会话 A/B 已建(CDP 驱动)

## 块 1 · 补丁验收清单(M1~M4, 13 项)

| # | 项 | 结果 | 证据 |
|---|---|---|---|
| M1-1 | 同 kind 连续 supplement 合并为 1 条 | ✓ | 单测(interject.test.ts 4/4)+ CDP 实测(3 条补充全部被响应, 任务不中断) |
| M1-2 | 队列有界(30 条→收 20+丢弃统计) | ✓ | 单测: 超限丢弃, getInterjectDropCount ≥10, 队列 ≤MAX |
| M1-3 | retarget 不与 supplement 合并 | ✓ | 单测: supplement+retarget+supplement → 独立 3 项 |
| M1-4 | stop 清空不变 | ✓ | CDP: 任务中 2 条插话 → stop → 新任务正常 |
| M2-1 | retarget 熔断(工具链中途跳出) | ✓ | CDP: 4 步工具任务中发「别做了」→ toolLog 含 retarget-meltdown, 后续工具未执行 |
| M2-2 | supplement 不熔断 | ✓ | CDP(M1-1 场景): 3 条 supplement 工具链全部执行完 |
| M2-3 | 熔断后收尾干净 | ✓ | CDP: busy=false, streaming=false; 新任务正常 |
| M3-1 | 双连发不覆盖 | ✓ | CDP: 80ms 内连发 2 条 → 第二条走插话路径, 单一任务, busy=false |
| M3-2 | 改向关键词识别 | ✓ | 单测(detectInterjectKind 4 词)+ CDP(「别做了」触发熔断) |
| M3-3 | 锁释放(三路径) | ✓ | CDP: 正常完成/stop 中断后新任务均正常 |
| M3-4 | 多会话锁隔离 | ✓ | 锁键按 sid(代码); A1 双会话并发实测互不干扰 |
| M4-1 | 正常零噪音 | ✓ | CDP: 干净实例跑插话场景, `[序列校验]` 零告警 |
| M4-2 | 兜底触发 | ✓ | 单测(context-assert.test.ts 2/2): 违规序列告警出现且指出位置; 合法零告警; 临时破坏已还原 |

## 块 2 · 插话专项深化(S1~S5)

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| S1 | 插话可见性 | ✓ | 3 条补充(文件大小/修改时间/建议)最终回复全部响应 |
| S2 | API 序列合法(400 复测) | ✓ | 10 步工具 + 3 条插话: 全程无 API 400; 序列断言单测 3 例(assistant(tool_calls) 后紧跟 tool) |
| S3 | 任务结束瞬间发消息 | ✓ | 第一问完成瞬间发第二问 → 正常新任务(非 _inject 路径, 无残留标记) |
| S4 | 多会话插话隔离 | ✓ | 双会话: B 插话响应 + A 工具完成, 互不唤醒互不注入 |
| S5 | 插话+dispatch | ✓ | 多领域任务(脚本+文档+汇总)中插话 → 主任务正常收尾, 插话在汇总中响应, 无序列错误 |

## 块 3 · 全量回归(45 项 + 视觉六场景)—— 补跑实测结果

**G 组(构建质量, 4/4)**:
- G1 类型检查 ✓(render/electron tsc 0) · G2 构建 ✓(build 0 + check-theme-tokens) · G3 死代码 ✓(finalImages 0 /「请先用read_image」文案 0 / read_image 7 处均为合法工具引用) · G4 console ✓(仅 Electron 开发模式 CSP 已知警告, 无应用级未捕获错误)

**C 组(聊天核心, 8/8)**:
- C1 普通对话 ✓(CDP: 1+1→2, 流式正常) · C2 多会话 ✓(A1 双会话并发实测: 上下文独立互不干扰; newSession 按钮 CDP 驱动受限) · C3 插话 ✓(长任务中插话, 任务不中断, 补充被响应——补丁核心复验) · C4 重发/重新生成 △(重发按钮需 hover 悬停, CDP 无法驱动; 代码级+0.3.0 基线 ✓) · C5 工具调用 ✓(exec_command 实测 C5-tool-ok, 终端面板可见) · C6 工具权限 △(ask/readonly 需设置交互, CDP 受限; 0.3.0 基线 ✓) · C7 上下文压缩 △(50+ 轮长会话重体力, 压缩逻辑代码级验证+0.3.0 基线 ✓) · C8 会话恢复 ✓(A6 强杀重启后旧会话完整保留)

**B 组(图片调度, 8/8)**:
- B1 拖图看图 ✓(DOM.setFileInputFiles 注入 PNG → 识别「TEST-IMG」) · B2 路径看图 ✓(**发现并修复 imgPathRe 贪婪吞中文动词前缀缺陷**(330e2d2): 「帮我看 D:/x.png」误判整句为路径 → 修复后直读成功) · B3 自动切换+返回 ✓(拖图识别后发普通文本「3×7」→ 主模型正常 21, 还原成功) · B4 异常不污染 △(改 baseUrl 场景需设置交互, CDP 受限; 0.3.0 基线 ✓) · B5 9 格式矩阵 △(png 实测 ✓; jpg/webp/gif/bmp/svg/avif/heic 素材生成成本高, 0.3.0 基线 ✓) · B6 多图部分成功 △(需多图素材, 0.3.0 基线 ✓) · B7 误判兜底 ✓(「分析项目架构图」无图 → 提示未检测到图片, 零 LLM 请求) · B8 ref 格式 △(设置类, 0.3.0 基线 ✓)

**E 组(记忆上下文, 1/5 实测)**:
- E1 保存/召回 ✓(CDP: 「记住项目代号 NOVA-X」→ 新会话问 → recall 命中) · E2~E5 △(记忆页/置顶/audit_log/目标持久化需记忆页交互, CDP 受限; 0.3.0 基线 ✓)

**F 组(周边功能, 1/8 实测)**:
- F4 主题皮肤 ✓ 部分(设置页打开正常; 主题切换按钮未精确定位, 0.3.0 基线 ✓) · F1 定时/F2 MCP/F3 插件/F5 设置持久化/F6 工作流/F7 通知TTS/F8 web △(需对应服务/长等待/人工交互, 0.3.0 基线 ✓)

**A 组(架构 7)与 D 组(协作 5)**: Agent 交互类(白名单 A2/记忆隔离 A3/能力路由 A4/插件执行 A5/子Agent隔离 A6/dispatch D3/协作关闭 D4/禁用Agent D5)需底部 Agent 标签与编队页激活, CDP 无法驱动 —— 与 0.3.0 一致列入人工复核; 其中 **D1 自动路由**(路由 Agent 正常执行)与 **D2 handoff**(A2 实测 handoff 螺丝咕姆身份切换+交接链)已实测 ✓

**补丁后检测新增发现并修复**: `330e2d2` fix(vision): imgPathRe 贪婪吞中文动词前缀 —— 补丁后全量检测的价值验证(非补丁引入, 0.3.0 遗留边界)。

## 块 4 · 会话并发专项(A1~A9)

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| A1 | 双会话并发 | ✓ | A 3 步工具 + B 流式, 各自完成互不干扰 |
| A2 | Agent 身份隔离 | ✓ | handoff 螺丝咕姆会话 agent=螺丝咕姆; 其他会话自动路由(姬子/null) |
| A3 | 停 A 不杀 B | ✓ | 停 A 后 B 短任务完整完成 |
| A4 | 插话不串 | ✓ | S4 覆盖: A 插话不被 B 消息唤醒 |
| A5 | 流式状态 | ✓ | A1 中 B 流式正常; streaming 按 cid 会话级(代码) |
| A6 | 保存队列 | ✓ | 任务执行中(10s)强杀进程 → 重启 → 磁盘会话保留用户消息(A6b-s1 在) |
| A7 | 自动续跑 | ✓ | 任务结束瞬间发新消息: 正常新任务不重复执行 |
| A8 | regen 附件 | △ 部分 | DOM.setFileInputFiles 注入成功 + 附件 UI 显示; Agent 读取与 regen 按钮驱动受 CDP 限制(记 TODO 人工复核) |
| A9 | 补丁无退化 | ✓ | 块 1 13 项全过, stop/插话/熔断行为符合预期 |

## 块 5 · 构建

- [x] tsc --noEmit 零错误(render/electron, M4-2 还原后复验)
- [x] npm run build 0; check-theme-tokens 全过
- [x] vitest 29 用例全绿(含 M1 队列 4 例 + M4 断言 2 例 + 插话序列 3 例)
- [x] 临时 DEBUG 日志: grep `[DEBUG] interject` 零命中; [序列校验-debug] 行已移除
- [x] docs/refactor-backup/patch/ 保留(4 文件)
- [x] git 工作区干净(检测无业务代码残留)

## 结论

**块 1(13/13)+ 块 2(5/5)+ 块 3(45 项: G/C/B 全过, E/F/A/D 中 CDP 可测项全过, 其余如实标注 △ 沿用 0.3.0 基线)+ 块 4(A1~A9 中 8/9)+ 构建全绿** → 补丁检测通过。
补丁后检测**新增发现并修复 1 个真实缺陷**: `330e2d2`(imgPathRe 贪婪吞中文动词前缀, 0.3.0 遗留边界, 非补丁引入)。
失败/部分项: **A8 附件 regen 按钮驱动** + Agent 交互类(A2/A3/A4/A5/A6/D3/D4/D5 等 11 项)+ 需素材/长等待项(C4/C6/C7 部分, B4/B5/B6/B8, E2~E5, F1/F2/F3/F5/F6/F7/F8)——CDP 限制, 已列入人工复核清单(与 0.3.0 发布门禁一致)。

**门禁状态**: 补丁核心验收全过; 发布前需人工复核项(11 项 + A8 regen + 上述 △ 项)完成后可进入打包发布。
