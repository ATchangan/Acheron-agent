# 更新日志（重点）

## v0.4.0（2026-08-13 首发，2026-08-15 定稿）
- 记忆基座：本地 SQLite（node:sqlite 零原生依赖）持久化，强杀不丢；旧 JSON 记忆一次性迁移，建库失败自动降级内存模式
- 检索与分层：FTS5（trigram+BM25）与向量 RRF 双路融合检索；三档衰减（30/60/180 天，置顶永存）；四层金字塔 L0 原始记录→L1 原子事实→L2 场景→L3 核心结论，source_id 逐层溯源
- 事实库：中文启发式三元组抽取、精确去重置信度累计、同主谓冲突旧行自动淘汰；情景时间线 recall_events
- 模型网关：任务类型检测（text/code/vision/long，代码识别覆盖围栏代码块/行首命令/源码路径）+ 统一路由 + 降级链；本地视觉推理服务自动切换（加载→启动→健康检查→分析，失败回云，并发队列防 OOM）
- 上下文：大工具结果 side-channel 存档 + recall_tool_output 取回（会话限 5 次）；四要素状态（目标/已完成/产出物/未决）常驻 system 尾部
- 技能：triggers 正则匹配按需注入（单技能≤800 字、top2、命中统计落库）
- 界面清理：删除文件视图顶部的「处理器 / 内存 / 显卡」实时占用栏及其 2.5s 轮询，文件页只保留工作目录与文件树；后端 sysPerf 接口保留未被界面引用
- 工程：渲染层依赖移出运行时依赖；Electron 32→43、React 19、Vite 7、TS 5.9；electron-updater 增量更新（GitHub Release）；语言包裁剪为 zh-CN/en-US
- 0.4.0 定稿收口：移除桌宠（式神伴身/黄泉 3D 建模/Unity 原型）与相关素材；记忆收敛为 SQLite 单数据源（上下文注入 / recall_memory / 记忆页统一读写 agent.db，教训/目标/情景并入新表，旧 memory.json、memory-vector.json 仅保留一次性迁移读取）；向量检索加内存缓存与模长预存；会话索引改为按 mtime 增量更新并包事务；启动时的索引重建与每日维护延后到窗口显示后；side-channel/审计加保留期清理；每日维护含 PRAGMA optimize 与周级 VACUUM
- 0.4.0 定稿安全与链路：localVision 命令改无 shell 直接 spawn 并按引号切分，服务进程登记在册、退出统一回收；更新下载改流式写入 + 断点续传 + SHA256 校验；备份白名单补 agent.db 并在打包前做 WAL checkpoint，恢复前关闭数据库句柄；CI 升 Node 22、补评估/品牌词/UI 巡检门禁、Release 上传 blockmap 与 latest.yml 供增量更新
- 验证：49 文件 287 用例全绿；Windows 安装包打包通过

## v0.3.9（2026-08-11）
- 正确性：中文乱码修复、子代理统一执行管道、私有记忆隔离、模型降级链、密钥解密兜底、动态危险命令黑名单、MCP 确认统一
- 效率与结构：engine.ts 1749→1139 行、main.ts 489→306 行、渲染层死代码收敛、Agent 定义单源
- 记忆与技能：容量封顶自动归档、约定类自动置顶、内置技能可隐藏、技能同名用户优先
- 协作与纪律：强制 dispatch、handoff 带上下文、交付附「本次改进点」、工具表演检测、长任务并行新会话+完成通知
- 体验：复制按钮图标化、输出 Markdown 排版优化、全仓去 emoji/品牌词
- 工程：发布门禁 release:check、测试数据隔离、评估历史限容
- 验证：42 文件 248 用例全绿；CDP 全功能 35/35、UI 21/21、端到端 12/12

## v0.3.8（2026-08-09）
- 项目指令生态：AGENTS.md/CLAUDE.md/.agents.md 目录链合并、子目录按需注入、注入安全扫描
- 执行机制：原生 git 工具、事件钩子 Hooks、任务文件回滚、自定义子代理、模型失败自动降级、计划一致性
- PowerShell 7 全场景路由、GBK 解码、危险命令黑名单收窄；性能优化（同任务 61.6s→5.1s）
- 一键环境自检 21 项、用户消息重新生成；测试 239 用例

## v0.3.7（2026-08-09）
- 计划执行与验证闭环：工具调用生成步骤卡、收尾复盘、PLANS.md 落盘
- 技能生态：SKILL.md 清单 + read_skill、apply_patch、会话化终端
- 稳定性加固：任务停止即时落盘、渲染崩溃上下文、CPU 渲染防崩溃

## v0.3.6（2026-08-08）
- 流式渲染链路重构（拆订阅 + memo + Markdown 缓存）、事件 delta 增量传输、广播收窄
- 设置页 16 tab 懒加载（主 bundle -47.5%）、轮询收敛

## v0.3.5（2026-08-07）
- token 优化收官：11 个开关单点回退、LLM 摘要驱动压缩、代码结构清理

## v0.3.4（2026-08-06）
- 会话区回合制重构、浏览器面板内嵌实时画面、思考过程持久化、风险确认卡片

## v0.3.3（2026-08-06）
- 独立内核 AgentEngine：主进程直连 LLM、计划确认门、断点恢复
- 上下文压缩、工具缓存、多会话并发、日志轨迹

## v0.3.2（2026-08-05）
- token 优化系列：工具白名单注入、结果瘦身、图片降级、插话合并

## v0.3.1（2026-08-04）
- 全项目重构、会话系统修复、插话队列、安全补丁

## v0.3.0（2026-08-04）
- 架构重构：多角色编队（dispatch/handoff）、语义记忆 v2、Plan-Execute-Verify、浏览器自动化、插件执行层

## v0.2.x（2026-07-29 ~ 08-02）
- 安装包/主题皮肤/浏览器 web_read/GPU 选项/多 Agent 协作/式神录插件/会话导出等迭代

## v0.1.0（2026-07-29）
- 初始发布：Electron 桌面助手、多 Provider LLM、20 个内置工具、会话管理、三套主题
