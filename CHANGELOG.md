# 更新日志

## v0.3.8（2026-08-09）
- **Windows 命令执行修复**：cmd 在部分系统（OEM 437）会把中文输出成 `?`，现改为含非 ASCII 的命令自动走 PowerShell（UTF-8 输出），纯 ASCII 简单命令仍走 cmd；系统提示新增「Windows 命令纪律」（只写 PowerShell 语法、禁止 bash 语法、中文路径用 PowerShell）
- exec_command 工具说明同步更新
- **任务失败归因**：任务失败时错误提示附带失败步骤与工具，计划卡可点击定位
- **一键备份/恢复**：设置 → 引擎 → 数据管理新增「备份数据/从备份恢复」（zip 打包 settings/tasks/memory/sessions/plans/skills 等，恢复含覆盖确认与越权防护）
- **日志归档轮转**：agent-trace 超 2MB 时归档旧文件而非截断，归档保留 7 天
- **网络安全加固**：web_fetch/web_read 拦截回环/链路本地/内网地址（SSRF 防护）；外部网络内容自动加「不可信指令」标记
- **多会话并发上限**：新增「同时运行任务上限」（默认 3），超限新任务提示等待；断点恢复同样受控
- **会话 schema 版本**：会话文件写入 schemaVersion=1，为未来迁移预留
- **执行机制与项目集成（本轮）**：
  - **项目指令兼容**：引擎自动读取 `AGENTS.md` / `CLAUDE.md` / `.agents.md`（优先 AGENTS.md），主流生态项目指令可直接生效
  - **Git 原生工具**：新增 `git(action, args?)`（status/diff/log/commit/stash/push/pull/checkout），系统提示要求改代码前先 status/diff、改后 diff 验证、确认后 commit，不再手拼 git 命令
  - **事件钩子 Hooks**：设置 → 引擎 → 任务可靠性新增事件钩子文本域，支持 `tool-before` / `tool-after` / `task-start` / `task-end` / `file-write`，注入 `HQ_EVENT/HQ_TOOL/HQ_SID/HQ_TASK_ID/HQ_RESULT/HQ_PATH/HQ_STATUS` 环境变量（每行 `事件=命令`，10s 超时、失败仅记日志不影响主流程）
  - **计划确认门完善**：计划阶段系统提示注入「计划阶段」约束，只放行只读/检索/规划工具；git 在计划阶段仅允许 status/diff/log 只读动作
  - **自定义子代理**：用户目录 `%APPDATA%\huangquan-agent\agents\*.json` 放 `{"名称":{role,prompt,tools,model?}}` 即注册自定义角色，与内置角色同机制合并
  - **任务文件快照回滚**：引擎写操作前记录原内容（≤50 个文件、单文件 >5MB 跳过），任务结束落盘 `rollback/<任务ID>.json`；聊天页出现「回滚文件改动」横幅，一键恢复任务开始前状态
  - **模型失败自动降级**：非可重试错误自动切换到同供应商其他模型，其次其他有 key 的供应商重试一次（每任务最多一次）；修复此前降级逻辑永远无法触发的死代码
  - **计划增量传输**：plan-update 只带变化的步骤 id，渲染层局部 patch，长任务计划卡不再整表替换
  - **崩溃观察**：近 7 天渲染崩溃 ≥3 次时系统通知建议切换 CPU 渲染；计划文档保留 30 天后自动清理
  - **修复**：系统提示「计划执行」段落重复拼接；自定义子代理默认字段合并
  - **测试**：新增 hooks 解析/执行、自定义子代理合并、rollback 恢复、计划确认门批准/拒绝流程测试，测试总量 33 文件 203 用例全绿
  - **收尾优化（本轮）**：计划阶段只读判断抽成纯函数并补测试（git 仅放行 status/diff/log，忽略大小写）；git 只读动作不再清空读缓存（commit/stash/push/pull/checkout 仍清）；Hooks 同一事件多条命令改为按配置顺序串行执行；crash.log 超 5MB 自动轮转为 crash.log.old，崩溃统计同时读当前档与轮转档
  - **计划一致性修复**：`update_plan` 步骤无 id 时按 label+tool 合并，重复调用不再产生一模一样的两条；计划步骤按 id 与“未认领 pending 同 label+tool”双重去重（引擎与渲染层增量合并都加防御）；完成/失败状态只信引擎实际执行结果，不再信任模型在 update_plan 里自报的 done/failed；每任务技能清单只扫描一次（不再每轮读盘）；新增回归测试（连续两次相同 update_plan 后计划仍为 2 步且保持 pending）
  - **效率优化（对照主流）**：系统提示精简（计划/Windows/Git 纪律合并压缩，省约 200-300 字/轮）；简单任务不再默认要求 `update_plan`（工具调用自动生成步骤卡，复杂任务或用户要求时才声明）；强制验证闭环从最多 2 轮降为 1 轮；主控角色新增「核心工具模式」（默认开，约 28 个常用工具，进阶工具可在 设置→工具 单独放行或关闭恢复全量），预计同样任务轮次减少 40%-60%
  - **执行可靠性修复（实测驱动）**：危险命令黑名单把 `format` 收窄为 `format c:`/`format /`，`Format-Table`/`Format-List` 等正常 PowerShell 命令不再被误拦；`ls/find/grep` 空结果（空目录/无匹配）不再误判为失败，避免模型反复换工具重试；cmd 输出按本地代码页(GBK)解码，中文不再乱码。实测同一任务从 61.6s/6 轮/5 次工具(含失败重试)降至 5.1s/2 轮/1 次工具
  - **一键环境自检**：设置 → 诊断 新增「一键环境自检」，覆盖 PowerShell 7/Windows PowerShell/cmd、工作目录、用户数据/会话/回滚目录、API 供应商与网络连通、磁盘/内存、技能、插件、浏览器内核、渲染模式、Git、MCP、本地服务共 19 项，全部只读并给出异常项的解决建议；新增 IPC + 单测
  - **项目指令升级（混合主流方案）**：发现语义——每目录按 `AGENTS.override.md` > `AGENTS.md` > `CLAUDE.md` > `.agents.md` 解析，git 根 → 工作目录逐层合并、深层靠后优先，合并上限默认 32 KiB（设置 → 引擎 → 项目指令上限可调），超限打截断标记不再静默丢内容；注入策略——启动只注入工作目录链，模型读取子目录文件时按需注入该目录（上溯最多 5 层）的规则（单文件 8k 上限、每会话每目录一次）；写作纪律——系统提示注明常驻文件建议 200 行内、重内容拆子目录；所有注入前做提示注入安全扫描，可疑文件跳过
  - **收尾二轮（全部七项）**：README 同步 0.3.8 功能文档；eval 冒烟扩展至 12 场景（新增 git 工具 / 子目录指令注入 / 回滚 / Hooks / 计划确认门）；超大 AGENTS.md 改为按字节前缀读取，不再整文件读入内存；新增 `init_project_docs` 一键生成项目指令工具（不覆盖已有文件）；Hooks 新增 task-stop / task-resume / compact-before / model-fallback 事件；项目指令支持单文件内 `paths` 路径作用域（frontmatter + 通配匹配）；设置类型统一收敛到 `electron/shared/settings-types.ts`（渲染层与引擎共用一份，消除双份定义）

## v0.3.7（2026-08-09）
- **计划执行**：工具调用自动生成执行计划（状态机：待办/执行中/完成/失败/中止/暂停），计划卡实时打勾、可折叠、点击跳转执行记录；复杂任务支持模型用 `update_plan` 自主声明/更新计划；任务收尾自动生成「执行计划复盘」（完成/失败/未执行明细）
- **PLANS.md 计划文档**：每个任务自动落盘 `plans/<任务ID>.md`，含 Goal / Progress / Surprises & Discoveries / Decision Log / Outcomes 五段，随断点恢复继续维护
- **验证强制闭环**：修改文件后未运行验证命令时，引擎自动注入验证请求（最多 2 轮）并记入决策日志；write 后用 read 确认即视为验证通过
- **技能生态兼容**：引擎注入已装载技能清单（SKILL.md 标准），新增 `read_skill` 工具按需读取技能全文与 scripts/references，市面通用技能包可直接使用
- **新工具**：`apply_patch` 多 hunk 结构化编辑（唯一匹配校验）；`terminal_open/run/close` 会话化终端（长驻进程、GBK/UTF-8 编解码、30 分钟空闲自动关闭）
- **工具层声明式重构**：schema（tool-specs）/ 执行器（tool-handlers）/ 分发器（tools）/ 权限（tool-permission）分层拆分，只读工具缓存真正启用
- **稳定性加固**：任务停止即时落盘（不再残留 running 僵尸任务）；settings 加载失败自动备份降级（不覆盖坏文件）；启动时自动清理超 24h 孤儿任务；渲染崩溃日志携带上下文与引擎轨迹；CPU 渲染模式禁用 GPU 加速防崩溃
- **工程质量**：测试 184 个（含工具分发器集成测试/任务存储测试/技能路径测试）；`npm run typecheck` 独立类型门槛；ESLint 渐进配置（历史死代码清零，0 error）；eval 冒烟 7 场景（工具/计划/apply_patch/技能/验证闭环）；CI 增加 lint/typecheck/test
- **移除**：任务 token 预算暂停提示（预算仅作硬上限，达到后本轮结束，不再弹「继续/结束」）；计划执行相关 emoji 图标改为线性 SVG/文字

## v0.3.6 性能优化系列（2026-08-08）
- **流式渲染链路重构**：ChatView 不再订阅 streamText，消息列表拆为独立 MessageList 组件（只订阅消息/流式/阶段状态）；ConversationTurn / ToolRow / UserBubble / AssistantBlock 全部 memo 化，按消息对象引用比较，流式 chunk 只重渲染当前块，历史回合零成本；历史 Markdown 解析结果按内容缓存（MemoizedMarkdown），长会话滚动不再重复解析
- **事件传输增量升级**：assistant-chunk 从全量累积文本改为 delta 增量（长回复 IPC 传输量从 O(n²) 降至 O(n)，10k 字符回复约 126 倍）；reasoning 与正文解耦降频（+300 字符或 300ms 一次）；flush 间隔自适应（正文超 4KB 后 40ms→90ms）
- **引擎事件广播收窄**：渲染层注册订阅后只向订阅窗口广播，不再每次遍历所有窗口；无订阅时回退全窗口兜底
- **设置页懒加载**：16 个 tab 全部 React.lazy 拆包，主 bundle 316KB→166KB（-47.5%），首屏只加载默认 tab
- **轮询收敛**：文件树多目录轮询合并为单例调度器（窗口隐藏整批暂停）；浏览器面板隐藏时暂停轮询、恢复可见立即补刷；定时任务页每秒 tick 下沉到列表内部，表单/头部不再整页重渲染
- **验证**：`npm run build` 通过（渲染层 + 主进程双端类型检查）；`npm test` 17 文件 108 用例全绿；CDP 冒烟 16 个设置 tab 全部 PASS、零 JS 异常

## v0.3.5 Token 优化系列收尾（2026-08-07）
- **代码结构清理**：巨型 UI 组件全部拆分（定时任务 809→181、藏书阁 698→226、技能 634→167、计划 575→319、式神 570→309、输入框 522→394，均为独立可回退提交）；主进程工具函数迁出 main.ts（534→419）；8 处空 catch 补日志；内联样式收敛 674→513 处并建立通用样式常量
- **上下文压缩全链路对齐主流方案**：
  - 触发依据：真实输入用量 + 发请求前估算（preflight）双重判断；小窗口模型阈值下限 75%
  - 阈值配置：全局百分比（30–95%）+ 绝对 token 必压线（可选）+ 按模型覆盖（`模型=百分比` 每行），压缩保留最近轮数可调、60 秒冷却
  - 压缩提示：压缩期间显示“正在压缩历史”状态；多次压缩后自动在摘要附代价提示，建议开新对话
  - 输出截断：保留已生成文本追加续写请求（最多 4 次，上限逐次翻倍），不再整段重生成；不完整工具参数仍拒绝执行
  - 记忆注入护栏升级为 6000 字符（头 4000 + 尾 1500 + 截断标记），关键信息不丢
- **上下文压缩升级为 LLM 摘要驱动**：新增「窗口阈值压缩」——按最近一次请求的真实输入 token 判断，接近模型窗口上限（默认 70%，可调）时把最旧完整轮次交给模型总结成摘要，保留最近 N 轮（默认 6，可调）与 60 秒压缩冷却；删除旧的规则式压缩（轮次折叠、估算触发 + 统计摘要），旧开关与设置项一并移除，仅保留极端情况下的硬截断兜底
- **输入框可用性修复**：角色页「聊天/工作人设」输入框此前在预设角色下只读、与“编写后自动切换为自定义”提示矛盾；现改为点击/聚焦即自动切换为自定义并可直接输入
- **界面版本号收敛**：除「关于」页外，所有界面不再显示版本标注（引擎页区块标题、工具页系统信息中的版本行已移除）
- **并行结果总量护栏（perf: parallelCap）**：并行工具结果超过 4 个且总字符 >6000 时，保留前 4 大结果全量、其余按 1500 字符瘦身（头 800 + 尾 500 + 关键行）；完整版仍进工具面板，不丢信息
- **性能开关区补全（perf: toolWhitelist / outputCap）**：工具白名单注入与输出上限分级接入开关读取点，关闭即回退旧行为；至此 11 个优化项全部可单点关闭（设置 → 引擎 → 流量与性能，默认全开、即时生效并持久化）
- **设置搜索**：新增「性能优化 / token 优化」关键词直达引擎页
- **基准脚本升级（T3）**：对比版本更新为 0.3.0 vs 0.3.5，报告含收益表 + 智力门禁 6 项检查表 + 失败任务二分归因区
- **文档化（T4）**：README 新增 Token 优化章节（11 开关清单、参数表、缓存友好约定、基准报告入口）；更新日志补齐全系列条目
- 版本定位：0.3.2 注入层 → 0.3.3 上下文层 → 0.3.4 度量层 → 0.3.5 可控层收官；总降幅目标 ≥50%（以基准报告实测为准），能力与质量零降级

## v0.3.4（2026-08-06）

界面重构 + 浏览器实时画面版。聊天区全面重构为回合制布局，浏览器面板内嵌实时画面，推理强度支持原生参数驱动。

- **会话区重构（回合制）**：每条用户消息与其后助手内容合并为一个回合，消息列居中限宽；用户消息右侧玻璃圆角气泡（长文本自动折叠、悬停展开、点击编辑）；助手回复平铺 Markdown（流式原地增长），悬停浮现操作栏（时间/token/耗时/朗读/重新生成/复制/引用）；工具调用改为扁平脚手架行；输入区与消息列同宽的玻璃 Dock
- **浏览器面板内嵌实时画面**：agent 浏览器会话从隐藏窗口改为 WebContentsView，主窗口内直接显示真实网页（替代截图轮询，100% 同步），顶部工具栏可操作；CPU 兼容模式自动切离屏截图双引擎；零额外安装体积（复用 Electron Chromium）
- **推理强度原生参数驱动**：DeepSeek/Kimi/智谱/豆包·火山/SiliconFlow 走 thinking 开关、OpenRouter 走 reasoning{enabled,effort}、OpenAI 推理模型/Grok/LM Studio 走 reasoning_effort，其余兼容服务由提示词兜底；「关闭思考」独立开关；「仅当前模型」档位覆盖；推理菜单纯中文（关闭/快速/标准/高/极高/最高）
- **思考过程持久化**：流式 reasoning_content 不再丢弃，随消息实时写入会话；助手消息顶部「思考过程」折叠块（执行中展开、完成后收起），历史会话重开保留
- **流式输出提速**：流式期间纯文本直出（不再每 90ms 重解析 Markdown），节流降到 50ms；流式文字收敛到独立子组件，聊天区其它部分不随 token 重渲染；刷新间隔自适应（33ms~300ms）；「微压缩（每轮小步）」默认开启，每轮把最旧问答折进运行摘要
- **输入框圆角卡片式**：860px 居中圆角卡片（输入区+工具条两行）；「+ 补充更多上下文」可选上下文区；工具条附件上传按钮（图片走视觉通道）；圆形发送键；模型选择器胶囊化
- **风险确认增强**：确认卡片移到输入框上方居中；新增「以后都批准」（按操作类型持久化，可管理撤销）；「本次任务都批准」保持新任务自动失效
- **移除任务拆分与进度卡**（按用户要求）：不再额外调用 LLM 生成子任务清单；dispatch 恢复并行分批执行；步骤展示沿用任务进程卡
- **主题一致性修复**：定时任务页等副页面硬编码深色全部改为跟随主题变量；系统窗口按钮按当前主题着色并实时同步（浅色/深色/自定义按亮度自适应）
- **滚动跟随修复**：发送指令后强制回到底部并跟随输出；用户手动上滑停止跟随；点击「回到底部」恢复
- **会话字号控制**：原「代码字号」改为「会话字号」（正文/输入框/代码/工具输出），新增「跟随界面」选项
- **全面复查**：停止可靠性（dispatch 中止守卫）、风险放行可管理、输入框防串台（切换会话清空补充上下文/附件）、冗余清理

验证：`npm run build` ✓ / vitest 全绿 ✓ / 6 套预设主题逐一截图验证 ✓ / CDP 实测（浏览器实时画面、消息区样式对齐、风险卡片、多轮记忆）✓

## v0.3.3（2026-08-06）

工作视图升级 + 独立内核版。Agent 主循环迁入主进程，工作过程以聊天流内步骤卡片展示；可靠性、安全、存储全面加固。

- **独立内核 AgentEngine**：Agent 主循环迁入主进程——LLM 直连、工具分发、上下文/记忆/模型调度/子任务全部由引擎接管，渲染层只消费事件流；断点落盘可恢复；计划确认门（实验）
- **工作步骤卡片**：移除右侧终端面板，AI 工作过程改为聊天流内的自然语言步骤卡片（工具实时状态 chips、可展开结果）；同一任务合并为「任务进程卡」；文件树/工作目录/系统信息集成到左侧「文件」视图
- **聊天界面极简终端式**：去头像/去气泡/去发送者，用户消息 `❯` 强调色、助手 Markdown 通栏；流式回复 Markdown 实时渲染 + 闪烁光标；回到底部按钮、复制最后回复、错误一键重试；会话置顶（永久保留）
- **可靠性根治**：兼容网关 `message.tool_calls` 整体下发形式；工具轮输出上限自适应（800→4096，截断自动放大重试）；空响应/工具调用丢失/输出截断自动重试（指数退避，最多 5 次）；流式 45s 无数据 / 240s 总超时自动断开重试；最终回复重复在架构上根治；多轮对话记忆修复、人设 ishiki 注入
- **风险操作软件内确认**：L2/L3 风险改为右下角确认卡片（拒绝/允许 + 「本次任务都批准」，60 秒自动拒绝）；原生 `titleBarOverlay` 窗口按钮
- **浏览器可交互**：`browse` 升级为可访问性快照（`@编号` 可交互元素）；新增 `browser_click` / `browser_type` / `browser_press` / `browser_scroll` / `browser_console` / `browser_vision`；每任务独立浏览器会话；URL 安全校验
- **存储与性能**：全部原子写；会话搜索 FTS5-lite 倒排索引；缓存自动释放（文件缓存 32MB/10min、web_read 200 条、已加载会话只留当前+最近 2 个、Chromium 缓存超阈值自动清理）；长命令可被「停止」递归打断
- **多 Agent 协作完善**：dispatch 并发上限生效（超出自动排队）、禁用角色拦截、子任务 token 预算与 usage 去重、角色专属模型；交接开关落实；协作上限全部动态自动调节
- **安全**：MCP 工具自动注入（`mcp__服务器__工具`）+ 逐工具权限（默认 ask）；主窗口渲染沙箱 + CSP；L2 风险确认降噪；记忆安全扫描

验证：`npm run build` ✓ / vitest 17 文件 95 用例全绿 ✓ / CDP 实测（多轮记忆、工具调用、插话原文、停止 0 重试、风险卡片、原生窗口按钮、真实游戏任务完整交付）✓

## v0.3.2（2026-08-06）

Token 优化系列：工具白名单注入/结果瘦身/记忆裁剪/工作流按需/轮次折叠/输出分级/图片降级/参数截断/跨任务归档/并行护栏/插话合并（11 开关集中管理，默认全开、单点回退）；内置能力：记忆安全扫描、冻结快照+使用率、AGENTS.md 项目指令、会话搜索、回复去重；关于页版本号动态化；模型读取分页/全选/自动探测与视觉模型补全；界面全中文化；移除符文工坊。

## v0.3.1（2026-08-05）

**会话修复（块 A~F）**
- 会话级并发状态模块（session-state.ts），取代全局状态，多会话并发互不串台（Agent 状态/插话队列/阶段气泡全部会话级）
- 停止/重发/自动续跑：终止只作用于当前会话（会话级任务代号 + abort 会话过滤）
- 发送幂等去重（同一内容 500ms 内重复发送忽略）
- 清空边界 / 中途保存（长任务每 30 秒自动落盘）
- 主进程保存队列：防抖合并、meta 写盘绑定、load 失败标记

**重构（块 G~N）**
- 主进程拆分：main.ts 108KB → 28KB，107 个 IPC handler 全部迁入 electron/ipc/ 18 个域文件（行为零变化、通道名零变化）
- SettingsView 拆分：165KB → 11.9KB 壳，13 个 tab 迁入 src/components/settings/
- chat.ts 拆分：51KB → 10.4KB，发送主逻辑迁入 chat-send.ts
- 94 处补丁注释清零；组件全部 ≤25KB；全库 any 清零（0 处）
- vitest 测试基座（8 个测试文件 29 个用例，`npm test` 全绿）

**补丁（安全加固 + 构建门禁）**
- workflow 工具防挂起：脚本未调用 `ctx.done` 或返回 Promise 后不 resolve 时，30 秒超时兜底 + 普通返回值自动收尾（原实现会永久卡住工具循环，stop 也救不回）
- 插件沙箱 fs 路径限制：`require('fs')` 白名单全部包一层工作目录校验，插件不能读取工作目录外的任意文件
- 构建类型门禁：`npm run build` 增加渲染层 `tsc --noEmit`（原来只查 electron 端，渲染层类型错误不会让 CI 失败）
- LLM 日志脱敏：失败日志不再输出用户消息内容（只保留 role/工具结构）；`[LLM]` 调试日志收敛到 `HQ_LLM_DEBUG` 环境变量
- 记忆安全扫描：保存记忆时检测 API Key / 授权头 / JWT 等 7 类敏感模式与 4 类 prompt 注入特征，命中即拒绝落盘
- 插话队列补丁 M1~M4（有界合并/改向熔断/发送锁/序列断言）此前已并入本版

## v0.3.0（2026-08-04）

- 类型基础重构：types.ts 成为全库统一类型来源，tsc --noEmit（strict + noImplicitAny）纳入构建门禁，历史约 90 条类型错误清零
- chat.ts 模块化拆分：主模块 + context / memory / router / subtask / runtime 六个模块
- Agent 实体化：7 位 Agent 有真实工具白名单与能力路由，子 Agent 上下文隔离，编队管理页白名单可编辑、可持久化
- 插件执行层：index.js 协议 + vm 沙箱（require 白名单、10s 超时、4KB 输出截断）+ 权限 ask 确认，插件工具注入 LLM
- 全局 any 清零：显式 any 全库移除，错误信息统一提取
- 工作目录自定义：exec_command 执行目录跟随设置，新路径自动创建；设置页改完文件树立即刷新（不再靠 5s 轮询）；「◎」气泡一键切换工作目录
- 供应商面板：点击供应商自动预填默认 BaseURL / API 类型（只填空字段），切换供应商前自动保存当前配置
- 图片需求自动切换：发图时模型不支持视觉则自动切到可用视觉模型（同供应商优先），无可用模型则提示
- 媒体自动生成：对话中遇到生图/生视频需求自动调用 media_img / media_video 工具，策略页可关闭自动生成
- 视觉任务强制队列：识图任务强制走「视觉理解」模型，失败自动顺位切换，任务完成自动切回主力模型
- 图片调度修复（FIX-A~G）：9 种图片格式直读视觉链路、拖入图统一压缩（≤1568px / ≤1.5MB）、HEIC 转换提示、模型还原 finally 全覆盖、视觉误判兜底

## v0.2.4（2026-08-03）

- RAG embedding 升级：嵌入引擎可配置，TF-IDF 退役
- 中文 bigram 向量分词，语义检索更准
- 情景记忆写盘防抖，memory 异步写入
- 自动更新：启动时检查 GitHub Releases 新版本并提示
- 设置页新增「关于」章节（版本信息、软件更新独立 tab）
- 新增 CI 构建工作流（.github/workflows/build.yml，推 tag 自动打包发布）
- 危险命令拦截增强、skills 路径白名单
- 历史截断保留摘要、插话队列会话归属、Promise 错误不再刷屏

## v0.2.3（2026-08-02）

**安全加固**
- API Key / customHeaders / webReadCookies 经 **DPAPI（safeStorage）加密落盘**，不再明文保存
- 修复命令注入：skills/plugins 安装改用 spawn + 白名单校验
- 修复技能预览 XSS：renderMarkdown 全量转义 + 协议白名单
- 修复会话路径穿越：会话 id 白名单校验；sandbox 权限路径规范化（防 .. 穿越）
- workflow 工具加固（限长 8KB + 严格模式）；mkdir 走 IPC（工作目录校验），不再拼 shell
- abort 按 requestId 精确中止（多会话并发互不误杀）
- 每工具权限表（ToolsView）接入 runTool，deny/ask 生效

**新功能**
- 独立浏览器窗口 + 使用中悬浮窗（hash 路由 #browser / #float）
- **TTS 语音朗读**（Windows SAPI，离线可用）
- 常驻无头浏览器 + 实时快照（agent 浏览时页面保持打开，前端可实时截图查看）
- 单实例锁（防止多实例并行干扰悬浮窗/窗口）
- 思考气泡模式：工具过程统一显示在「思考气泡」内，消息流保持干净
- 大图压缩（≤1280px JPEG 0.8），避免本地视觉模型超时 + 会话文件膨胀

**修复与性能**
- read 工具 offset/limit 透传主进程分段读（>5MB 续读）
- grep/find 异步化（fs.promises）+ glob 转义修复 + 扩展名正则修复
- recall_memory 接入向量语义检索 + 关键词合并
- 会话元数据缓存（避免 list 全量解析大会话）
- MCP SSE 改用 net.fetch（跟随系统代理）
- 时间戳置于 prompt 绝对末尾，缓存前缀稳定
- 崩溃日志异步追加；清理主进程双 Agent 体系与 planner/workflow 死代码（6 文件）

**安装包修复（0.2.3）**
- 修复安装版启动崩溃：skillsDir 移至 userData（原指向 app.asar 内目录，mkdir 抛 ENOTDIR 导致主窗口不创建）
- 修复 exe/快捷方式图标：移除 signAndEditExecutable:false（会跳过 exe 资源编辑）+ 多尺寸 icon.ico

## v0.2.2（2026-08-01）

**浏览器与网页解析**
- 新增 **web_read 网页解析工具**（playwright-core + 系统 Edge 内核，无需额外安装浏览器）
- 浏览器设置合并为单一「浏览器」卡片（3 个子分组）

**渲染与性能**
- **GPU 渲染加速选项**：自动识别（auto / gpu / cpu 三档），RTX 系列实测硬件加速生效
- 流式渲染 40ms 节流，长回复更流畅
- 会话保存异步写盘，不再卡界面

**文件浏览器**
- 右侧工作目录升级为 **FileTree 文件浏览器**：展开/折叠、双击打开、悬停重命名/删除、文件夹+/文件+ 新建
- 原生右键菜单（复制路径带引号），写操作限工作目录内

**稳定性与配置**
- 修复 Anthropic 鉴权：自动识别 x-api-key 而非 Bearer
- settings.json 大字段剥离至 bgimage.dat（背景图与配置分离，避免每次保存全量写大文件）
- Token 优化：工具结果截断（8000/3000/6000）、历史 40 条上限、记忆限量

**打包**
- 打包方式由 Portable 改为 **NSIS 安装包**（可选安装目录、桌面快捷方式）
- 安装包内置 resources（人设 ishiki.md + 4 组技能），安装后功能完整
- 全新安装为空白配置，API Key 等均需重新填写

## v0.2.1（2026-08-01）

**稳定性**
- 全局崩溃捕获：主进程异常记录 crash.log 不再直接崩溃
- 禁用 GPU 硬件加速，防止 GPU 进程崩溃导致窗口渲染异常
- 渲染进程崩溃自动恢复（render-process-gone 监听 + 自动重载）
- IPC 安全序列化：消除循环引用 / Proxy / 不可序列化对象导致的报错
- 文件读取 UTF-8 多字节边界修正，不再截断中文
- 图片读取限制 20MB、目录扫描限制深度 8 / 5000 文件，防大目录阻塞

**Agent 与 LLM**
- Agent 编队改为**星穹铁道角色**（姬子/三月七/银狼/艾丝妲/知更鸟/黑天鹅/螺丝咕姆）
- AbortController 替代全局标志位，支持并发请求与多工具调用累积
- 新增 `llm:chatOnce` 非流式调用：多 Agent 分发时子 Agent 独立执行
- 新增 `llm:vision` 视觉辅助接口：主模型不支持多模态时自动切视觉模型
- 新增 `media:describe` 多媒体能力探测（LM Studio 本地视觉 / 即梦 / Agnes / 可灵）
- 修复非 DeepSeek Provider 全部报「不支持的 Provider」（支持 OpenAI Compatible 类型）
- 支持自定义请求 Headers（JSON 或 key=value）
- 修复 baseUrl 已含 /v1 或 /v4 时路径重复问题

**功能**
- 对话历史导出（md / json / txt 到工作目录）
- 清空全部对话历史、恢复出厂设置
- 最小化 / 关闭缩至托盘设置
- 文件权限四档（full / sandbox / readonly / ask）
- 工具开关（禁用列表过滤）
- 真实存储统计（会话/记忆/插件/缓存/工作区/设置）
- 定时任务基于计划时间计算下次触发，防止漂移累积
- 写操作时同步失效缓存

## v0.2.0（2026-07-30）

- 多 Agent 协作系统：7 角色编队 + handoff 交接 + dispatch 并行分发
- Plan-Execute-Verify 执行循环
- 语义记忆系统 v2（TF-IDF + 余弦相似度 + 衰减遗忘 + Token 预算）
- 上下文窗口智能管理（分层压缩 + Token 估算）
- 工具结果缓存（LRU+TTL，写操作自动失效）
- MCP 客户端（stdio + SSE 双传输）
- 工作流模板系统（6 个内置模板）
- L0-L4 风险分级权限 + 危险命令黑名单
- 8 个新 UI 组件 + 皮肤系统（背景图自动提色）
- Skills 扩充至 4 组 + 式神录插件系统
- 修复 portable 模式 mkdirSync 启动崩溃

## v0.1.0（2026-07-29）

- 初始发布：Electron + React + TypeScript
- 多 Provider LLM 支持 + 流式对话
- 20 个内置工具 + 浏览器自动化 + 代码沙箱
- 会话管理 + 系统托盘 + 三套主题
- 聊天/工作双模式切换
