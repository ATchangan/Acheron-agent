# 更新日志

## v0.3.4（2026-08-06）

界面重构 + 浏览器实时画面版。聊天区全面重构为回合制布局，浏览器面板内嵌实时画面，推理强度支持原生参数驱动。

- **会话区重构（回合制）**：每条用户消息与其后助手内容合并为一个回合，消息列居中限宽；用户消息右侧玻璃圆角气泡（长文本自动折叠、悬停展开、点击编辑）；助手回复平铺 Markdown（流式原地增长），悬停浮现操作栏（时间/token/耗时/朗读/重新生成/复制/引用）；工具调用改为扁平脚手架行；输入区与消息列同宽的玻璃 Dock
- **浏览器面板内嵌实时画面**：agent 浏览器会话从隐藏窗口改为 WebContentsView，主窗口内直接显示真实网页（替代截图轮询，100% 同步），顶部工具栏可操作；CPU 兼容模式自动切离屏截图双引擎；零额外安装体积（复用 Electron Chromium）
- **推理强度原生参数驱动**：DeepSeek/Kimi/智谱/豆包·火山/SiliconFlow 走 thinking 开关、OpenRouter/Nous 走 reasoning{enabled,effort}、OpenAI 推理模型/Grok/LM Studio 走 reasoning_effort，其余兼容服务由提示词兜底；「关闭思考」独立开关；「仅当前模型」档位覆盖；推理菜单纯中文（关闭/快速/标准/高/极高/最高）
- **思考过程持久化**：流式 reasoning_content 不再丢弃，随消息实时写入会话；助手消息顶部「思考过程」折叠块（执行中展开、完成后收起），历史会话重开保留
- **流式输出提速**：流式期间纯文本直出（不再每 90ms 重解析 Markdown），节流降到 50ms；流式文字收敛到独立子组件，聊天区其它部分不随 token 重渲染；刷新间隔自适应（33ms~300ms）；「微压缩（每轮小步）」默认开启，每轮把最旧问答折进运行摘要
- **输入框圆角卡片式**：860px 居中圆角卡片（输入区+工具条两行）；「+ 补充更多上下文」可选上下文区；工具条附件上传按钮（图片走视觉通道）；圆形发送键；模型选择器胶囊化
- **风险确认增强**：确认卡片移到输入框上方居中；新增「以后都批准」（按操作类型持久化，可管理撤销）；「本次任务都批准」保持新任务自动失效
- **移除任务拆分与进度卡**（按用户要求）：不再额外调用 LLM 生成子任务清单；dispatch 恢复并行分批执行；步骤展示沿用任务进程卡
- **主题一致性修复**：定时任务页等副页面硬编码深色全部改为跟随主题变量；系统窗口按钮按当前主题着色并实时同步（浅色/深色/自定义按亮度自适应）
- **滚动跟随修复**：发送指令后强制回到底部并跟随输出；用户手动上滑停止跟随；点击「回到底部」恢复
- **会话字号控制**：原「代码字号」改为「会话字号」（正文/输入框/代码/工具输出），新增「跟随界面」选项
- **全面复查**：停止可靠性（dispatch 中止守卫）、风险放行可管理、输入框防串台（切换会话清空补充上下文/附件）、冗余清理

验证：`npm run build` ✅ / vitest 全绿 ✅ / 6 套预设主题逐一截图验证 ✅ / CDP 实测（浏览器实时画面、消息区样式对齐、风险卡片、多轮记忆）✅

## v0.3.3（2026-08-06）

工作视图升级 + 独立内核版。Agent 主循环迁入主进程，工作过程以聊天流内步骤卡片展示；可靠性、安全、存储全面加固。

- **独立内核 AgentEngine**：Agent 主循环迁入主进程——LLM 直连、工具分发、上下文/记忆/模型调度/子任务全部由引擎接管，渲染层只消费事件流；断点落盘可恢复；计划确认门（实验）
- **工作步骤卡片**：移除右侧终端面板，AI 工作过程改为聊天流内的自然语言步骤卡片（工具实时状态 chips、可展开结果）；同一任务合并为「任务进程卡」；文件树/工作目录/系统信息集成到左侧「文件」视图
- **聊天界面极简终端式**：去头像/去气泡/去发送者，用户消息 `❯` 强调色、助手 Markdown 通栏；流式回复 Markdown 实时渲染 + 闪烁光标；回到底部按钮、复制最后回复、错误一键重试；会话置顶（📌 永久保留）
- **可靠性根治**：兼容网关 `message.tool_calls` 整体下发形式；工具轮输出上限自适应（800→4096，截断自动放大重试）；空响应/工具调用丢失/输出截断自动重试（指数退避，最多 5 次）；流式 45s 无数据 / 240s 总超时自动断开重试；最终回复重复在架构上根治；多轮对话记忆修复、人设 ishiki 注入
- **风险操作软件内确认**：L2/L3 风险改为右下角确认卡片（拒绝/允许 + 「本次任务都批准」，60 秒自动拒绝）；原生 `titleBarOverlay` 窗口按钮
- **浏览器可交互**：`browse` 升级为可访问性快照（`@编号` 可交互元素）；新增 `browser_click` / `browser_type` / `browser_press` / `browser_scroll` / `browser_console` / `browser_vision`；每任务独立浏览器会话；URL 安全校验
- **存储与性能**：全部原子写；会话搜索 FTS5-lite 倒排索引；缓存自动释放（文件缓存 32MB/10min、web_read 200 条、已加载会话只留当前+最近 2 个、Chromium 缓存超阈值自动清理）；长命令可被「停止」递归打断
- **多 Agent 协作完善**：dispatch 并发上限生效（超出自动排队）、禁用角色拦截、子任务 token 预算与 usage 去重、角色专属模型；交接开关落实；协作上限全部动态自动调节
- **安全**：MCP 工具自动注入（`mcp__服务器__工具`）+ 逐工具权限（默认 ask）；主窗口渲染沙箱 + CSP；L2 风险确认降噪；记忆安全扫描

验证：`npm run build` ✅ / vitest 17 文件 95 用例全绿 ✅ / CDP 实测（多轮记忆、工具调用、插话原文、停止 0 重试、风险卡片、原生窗口按钮、真实游戏任务完整交付）✅

## v0.3.2（2026-08-06）

Token 优化系列：工具白名单注入/结果瘦身/记忆裁剪/工作流按需/轮次折叠/输出分级/图片降级/参数截断/跨任务归档/并行护栏/插话合并（11 开关集中管理，默认全开、单点回退）；主流桌面 Agent 优点吸收（记忆安全扫描、冻结快照+使用率、AGENTS.md 项目指令、会话搜索、回复去重）；关于页版本号动态化；模型读取分页/全选/自动探测与视觉模型补全；界面全中文化；移除符文工坊。

## v0.3.1（2026-08-05）

- 会话级并发状态重构（多会话互不串台）；停止/重发/自动续跑只作用于当前会话
- 主进程拆分（main.ts 108KB → 28KB，107 个 IPC handler 迁入 18 个域文件）；SettingsView 拆分（13 tab）；chat.ts 拆分
- 安全加固：workflow 工具防挂起（30s 超时兜底）、插件沙箱 fs 路径限制、构建类型门禁、LLM 日志脱敏
- 全库 any 清零；vitest 测试基座引入

## v0.3.0（2026-08-04）

- 类型基础重构（types.ts 统一类型来源，tsc strict 构建门禁）
- Agent 实体化：7 位 Agent 真实工具白名单与能力路由、上下文隔离、白名单可编辑持久化
- 插件执行层（vm 沙箱、require 白名单、10s 超时、4KB 截断）
- 工作目录自定义、供应商面板预填、图片需求自动切换视觉模型、视觉任务强制队列

## v0.2.4（2026-08-03）

- RAG embedding 可配置、中文 bigram 向量分词；情景记忆写盘防抖
- 自动更新（启动检查 GitHub Releases）；设置页「关于」章节；CI 构建工作流
- 危险命令拦截增强、skills 路径白名单

## v0.2.3（2026-08-02）

- 安全加固：API Key/customHeaders/webReadCookies DPAPI 加密落盘；命令注入修复；XSS 转义；路径穿越修复；abort 精确中止
- 新功能：独立浏览器窗口+悬浮窗、TTS 语音朗读（Windows SAPI）、常驻无头浏览器+实时快照、单实例锁、思考气泡模式
- 性能：read 分段读（>5MB 续读）、grep/find 异步化、会话元数据缓存、MCP SSE 跟随系统代理
- 安装包修复：skillsDir 移至 userData（解决安装版启动崩溃）、多尺寸 icon.ico

## v0.2.2（2026-08-01）

- web_read 网页解析工具（playwright-core + 系统 Edge）；GPU 渲染加速选项（auto/gpu/cpu）；流式渲染 40ms 节流
- 右侧 FileTree 文件浏览器；修复 Claude 鉴权（x-api-key）；settings.json 大字段剥离
- 打包改为 NSIS 安装包（可选目录、桌面快捷方式），内置人设与技能

## v0.2.1（2026-08-01）

- 全局崩溃捕获 + 渲染进程崩溃自动恢复；IPC 安全序列化
- Agent 编队改为星穹铁道角色；AbortController 并发请求；llm:chatOnce/llm:vision 接口
- 对话历史导出、清空/恢复出厂、托盘设置、文件权限四档、工具开关、真实存储统计

## v0.2.0（2026-07-30）

- 多 Agent 协作系统（7 角色编队 + handoff + dispatch）；Plan-Execute-Verify 执行循环
- 语义记忆系统 v2（TF-IDF + 余弦相似度 + 衰减遗忘）；上下文窗口智能管理；工具结果缓存
- MCP 客户端（stdio + SSE）；工作流模板（6 个）；L0-L4 风险分级权限；皮肤系统；Skills 4 组 + 式神录插件

## v0.1.0（2026-07-29）

- 初始发布：Electron + React + TypeScript；多 Provider LLM 支持 + 流式对话
- 20 个内置工具 + 浏览器自动化 + 代码沙箱；会话管理 + 系统托盘 + 三套主题
