# 更新日志（重点）

## v0.4.4（2026-08-26 ~ 08-29）

### 精简回归

- 能力收敛：仅保留**工具调用 / 插件 / 会话流式回复**；技能、多角色编队、长效记忆、知识库、定时任务、命令中心、多媒体生成、配置档案、API Keys 独立页、诊断与模型缓存统计已移除
- 界面按开发版重写：左侧会话（聊天 / 工作双模式 + 置顶 / 归档 / 搜索）+ 底部导航（对话 / 浏览器 / 文件 / 设置）
- 设置页重组：供应商 / 策略 / 人格 / 工具 / MCP / 插件 / 外观 / 界面 / 快捷键 / 引擎 / 关于
- 引擎对齐：系统提示词改为单助手；默认工具集覆盖全部内置工具并保留风险确认与权限分级
- 长任务感知性：`task-progress` 事件 + 计划卡进度 + 无进展停滞兜底
- 工程纪律：`tsc` 0 报错（双 tsconfig）、lint 0 错误、单测同步对齐

### 自主闭环 + 会话体验重构

- **记忆系统回归**：本地记忆网关 sidecar（SQLite）；任务完成自动沉淀对话；模型按需调用记忆/对话/技能检索工具
- **定时任务回归 + 文件监控触发器**：5 段 cron / @daily 快捷式 / 监控文件与目录；触发后在专属会话执行
- **技能中心回归**：列表 / 启停 / Git 安装 / 新建 / 删除 / 使用统计
- **会话精修**：思考行计时 / 工具输出渐隐展开 / 用户气泡粘性吸附 / 会话内查找（Ctrl+F）
- **导航与性能**：右缘 prompt 时间线 / 渲染预算分页 / 流式批处理（60ms 合批）
- **省 token**：命令输出两端保留；>2KB 工具结果自动存档 side-channel；上下文构建层瘦身
- **可靠性与安全**：命令注入封堵、协议白名单、原子写、db 损坏隔离、启动对账兜底、确定性错误不再重试

### 界面改版：深色工作台

- **首页**：黄泉立绘头像 + 简洁品牌名 + 模式副标题（原创设计）
- **Archeron 主题**（墨蓝底 + 雾紫字 + 近白强调）并设为深色默认
- **侧栏改版**：会话 / BOTS 双页签 + 功能菜单 + 底部图标排
- **输入框**：单行设计，模型选择器（搜索 / 供应商分组 / 刷新 / 编辑）
- **标题栏**：布局编辑器 / HUD 模式 / 动效反馈 / 设置 / 右侧栏
- **品牌资源**：应用图标换为黄泉立绘多尺寸渲染

### 会话与输入框对齐

- **排队输入**：任务运行中发送的消息排队为后续修改，回合结束自动依次执行
- **活动行栈**：工具调用按类别合并折叠（编辑 / 终端 / 浏览器 / 检索 ×N）
- **改动摘要卡**：N 个文件已更改 +增/-删，可展开清单
- **问题跳转**：右缘编号时间线
- **权限档盾牌下拉**：只读 / 操作前询问 / 自动审核 / 完整权限，一键切换
- **模型选择器**：搜索 / 供应商分组 / 刷新模型 / 编辑模型…

### 新功能页

- **BOTS**：预设角色助手（聊天式列表 + 一键发起会话）
- **消息平台**：QQ 官方机器人接入（WebSocket + 被动回复）
- **任务**：并行任务管理 + 历史记录
- **产物**：跨会话产物表格（文件/链接筛选 + 分页）
- **当日总结**：右缘竖排页签（今日会话/任务/token）
- **配置档案条**：设置快照/切换/导入
- **已归档对话**：归档列表 + 恢复/删除

### 设置

- 全屏弹窗 + 顶部搜索 + 17 项平铺导航
- 模型页：默认模型 + 辅助模型行式布局 + 默认值推理
- 通知：任务完成 + 消息平台来信系统通知
- 记忆与上下文：持久记忆 / 用户画像 / 记忆预算 / 压缩阈值 / 压缩目标 / 保护最近消息
- 账单：按模型用量统计表 + 缓存命中率
- 网关：QQ 消息网关状态 + 重启
- 工作区 / 安全 / Browser：真实设置页

## v0.4.3（2026-08-23）技能生态

- 技能编辑器与校验：4 规则校验，写盘路径白名单防穿越；新增 `skills:validate` / `skills:write` / `skills:stats`
- 命中统计：按日聚合的 `skill_stats`（hit / trigger / ok），设置页展示近 30 天命中率 / 触发率 / 成功率
- 模板库 5 → 10：新增 json-validator / log-troubleshoot / csv-clean / api-doc-gen / backup-script 五个内置技能
- 注入预算监控：命中技能超 2 个按相关度截断并记录 `skill.budget-truncate` trace
- 更名与品牌：应用更名为 Acheron-Agent（窗口 / 标题 / 安装包产物名），黄泉人设插画用于欢迎 / 侧栏 / 关于 / 角标；去除第三方品牌词
- 界面：输入区下拉点外部自动收回；若干渲染稳定性修复
- 性能与工程：Vite 分包优化（主 bundle ~1.14MB → 346KB）；`noUnusedLocals / noUnusedParameters` 全开，`tsc` 0 报错；移除多余依赖（含 `concurrently`）；全局错误捕获 + 渲染崩溃自动重建
- 336 项测试全绿

## v0.4.2（2026-08-21）UI 重构与稳定性加固

- 界面重构：标题栏 / 侧栏 / 状态栏 / 设置页 / 路由浮层全面重排，输出统一为流式 Markdown 渲染，审批卡内联展示
- 稳定性：渲染进程崩溃后重建窗口自动恢复，工具执行超时兜底，错误边界重构，Electron 升至 43.4.1
- 清理与验证：删除冗余组件与死代码

> 更早版本见 Git 历史。
