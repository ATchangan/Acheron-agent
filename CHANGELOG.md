# 更新日志（重点）

## v0.4.4 界面改版：深色工作台 + 会话对齐（2026-08-29）

### 会话与输入框对齐

- **输入框两层 composer**：上层输入文字，下层控件行 —— 左侧 + 菜单（上传图片/文件/补充上下文/快捷指令）与权限档盾牌下拉（只读/操作前询问/自动审核/完整权限，一键切换）；右侧 上下文用量（已用/总量，随流式增长）/ 模型 pill / 推理档 / 圆形发送钮
- **排队输入**：任务运行中发送的消息排队为「后续修改」（占位文案「继续输入以排队后续修改」，队列 chips 可移除，回合结束自动依次执行）；「别做了/停止」等改向指令仍即时打断；手动中止会清空队列
- **活动行栈（工具调用）**：一轮内同类工具合并为一行折叠行（编辑/终端/浏览器/检索 + 最新参数摘要 + ×N），思考为独立行；运行中组头呼吸 + 「工作中 N分N秒」计时；点行展开全部调用详情；dispatch 委派卡片保留
- **改动摘要卡**：N 个文件已更改 +x/-y（从编辑类工具调用统计），可展开每文件增删行数清单；回滚仍走任务级快照（rollback.apply）
- **问题跳转**：右缘时间线改编号样式（跳转到第 N 条问题）
- **新页面**：任务（各会话进行中任务进度 + 历史任务记录，可跳转会话）、插件市场（插件库独立入口）、定时任务、产物、技能与工具、消息平台（QQ 官方机器人）
- **消息平台**：接入 QQ 官方机器人（q.qq.com 开放平台）—— WebSocket 网关 + 被动回复；每个联系人/群一个专属会话，回复自动发回；微信按「先只做 QQ」决策预留占位
- **BOTS 页签**：预设角色助手（聊天式列表：头像/状态点/最后消息预览，新建/编辑/删除 Bot 人设，点卡片进入专属会话）
- **侧栏底部配置档案条**：设置快照/切换/导入/删除（default 档案 + 新建 + 导入 + 管理）
- **右缘「当日总结」**：今日活跃会话/完成任务/输出 token 指标卡 + 会话与任务明细
- **设置真实页**：通知（任务完成/消息平台来信系统通知开关，引擎与 QQ 连接器已接）、记忆与上下文（记忆内核状态 + 上下文窗口覆盖）、账单（按模型用量统计表 + 缓存命中率）、网关（QQ 消息网关连接状态 + 重启网关）、Browser（渲染内核状态）；语音相关内容全部移除；消息平台渠道收敛为 QQ Bot（已接入）+ 微信（暂缓）
- **标题栏对齐**：布局编辑器（侧边栏/右栏/状态栏/当日总结/配置档案条 显隐开关 + 命令面板入口）、HUD 模式（迷你常驻输入条小窗，Esc 关闭）、动效反馈（全局动画开关）
- **输入框对齐**：单行 composer `+ 从一个目标开始 …… 模型 ●˅ ↑`；模型选择器重做 —— 顶部搜索、按供应商分组、选中 ✓、底部「刷新模型」（真实调用 detect 合并列表）/「编辑模型…」（跳设置·提供方）；推理默认档收进 设置→模型「默认值·推理」，权限档收进 + 菜单（单选高亮），上下文用量在状态栏
- **设置**：供应商页顶部新增「默认模型（应用于新会话）」下拉，与输入框模型选择器写同一配置

### 深色工作台风格

- **首页 hero**：大衬线字 ARCHERON AGENT（内嵌 OFL 字体，本地加载）+ 中文副标题，替换原头像式空态；聊天/工作模式副文案区分
- **新增 Archeron 主题**（墨蓝底 + 雾紫字 + 近白强调）并设为深色默认；跟随系统与原「深色」预设自动解析到该主题，外观页可切换，其余主题不变
- **悬浮 composer**：单行圆角输入卡（+ 菜单聚合 上传图片/文件/补充上下文/快捷指令/文件权限），右侧模型 pill + 推理档 + 圆形发送钮；原双行工具栏收进菜单，行为不变
- **侧栏改版**：聊天/工作页签化 + 菜单行（新建会话 Ctrl N / 技能与工具 / 产物 / 定时任务）+ 会话空态（暂无会话 + 新建项目选工作目录）+ 底部图标排（对话/浏览器/文件/命令面板/设置）
- **三个功能页回归（浮层路由）**：定时任务（cron/文件监控 全生命周期管理）/ 产物（工作目录文件树浏览）/ 技能与工具（内置工具 55 项 + MCP + 插件聚合）
- **标题栏**：左侧仅侧栏开关与面板翻转，右簇五图标；会话有消息才显示标题；状态栏版本号改 `# v` 样式
- **设置页**：策略 tab 重组为「辅助模型」行式布局（长文本/代码/快速响应 + 调度绑定，右侧下拉即改）；图标导航视觉对齐
- **品牌资源**：应用图标（窗口/托盘/安装包）更换为黄泉立绘多尺寸渲染

## v0.4.4 自主闭环 + 会话体验重构（2026-08-29）

- **记忆系统回归（全新内核）**：本地记忆网关 sidecar（应用自动拉起，SQLite 存储在应用数据目录）；任务完成自动沉淀对话（L0→L1 原子→L2 场景→L3 画像异步归纳）；模型按需调用 记忆检索/对话检索/技能检索 工具；对话检索带本地会话全文检索兜底
- **定时任务回归 + 文件监控触发器**：5 段 cron / @daily 快捷式 / 监控文件与目录（内容 hash 抑制，变化才触发）；模型可对话内创建（schedule_task），设置→引擎 提供管理卡片；触发后在专属会话执行
- **技能中心回归**：列表 / 启停 / Git 安装 / 新建 / 删除 / 使用统计；引擎关键词注入照旧
- **会话界面重构（聚合工作流）**：多轮工具调用折叠为单行摘要（"编辑了 2 个文件 · 运行了 6 条命令"），运行中摘要呼吸 + 工具活动单行滚动；最终回复独立带完整操作条；中间步骤操作条悬停才显
- **会话精修**：思考行计时（思考了 Ns）/ 工具输出渐隐展开（>120px 折叠）/ 用户气泡粘性吸附（两行钳制悬停展开）/ 脚手架整体淡化（悬停全亮）/ 会话内查找（Ctrl+F 原生高亮计数）
- **导航与性能**：右缘 prompt 时间线（≥4 回合，点击跳转）/ 渲染预算分页（长会话只挂载最近 16 回合，"加载更早"回填）/ 工具详情内 markdown 正经渲染
- **流式批处理**：delta 与推理增量按 60ms 合批落 store（40-70 chunk/s 逐条更新会打满主线程）；订阅隔离（列表不再逐 chunk 重渲染，跟随滚动改事件驱动）
- **省 token（结构化，非截断）**：命令输出超长改"头 6000 + 尾 1500"两端保留（尾部错误信息不再丢失）；>2KB 工具结果自动存档 side-channel，上下文仅留取回指针（recall_tool_output 按需取回）；上下文构建层 头+关键错误行+尾 瘦身
- **委派卡片**：dispatch 子任务列出 角色+摘要+状态
- **可靠性与安全**：kill_process/sid/codebox 命令注入封堵、browser:open 协议白名单、原子写改 rename+fsync、db 损坏隔离、启动对账兜底（防误删全部会话）、确定性错误不再重试（风险拒绝不连弹）、任务完成通知点击聚焦并跳转会话、引擎/网关 Map 泄漏清理
- **视觉规格对齐**：全局令牌化密度体系（正文 13px/1.5、工具行 11px/18px 行盒、块距 12px、段距 11px、工具行距 6px）；用户气泡 rounded-xl 12/8；操作条静息 50% 悬停全亮；脚手架文字降为 54% 灰；输入卡玻璃增强（blur+饱和）

## v0.4.4（2026-08-26）精简回归：仅保留 工具调用 / 插件 / 会话流式回复

- **能力收敛**：技能（skills）、多角色编队（agents/手off/dispatch）、长效记忆（记忆页/回忆/教训沉淀）、知识库（藏书阁）、定时任务（cron 调度器与页面）、命令中心与工作流、多媒体生成（图片/视频）、配置档案、API Keys 独立页、诊断与模型缓存统计全部移除；导航仅保留 对话 / 浏览器 / 文件 / 设置
- **界面按开发版重写**：标题栏 / 左侧会话（聊天 vs 工作双模式、置顶 / 归档 / 搜索 / 拖拽排序 / 项目区）/ 底部图标导航 / 状态栏（模式 / 模型 / 上下文 / 长任务进度与停滞提示）
- **设置页重组**：供应商 / 策略 / 人格 / 工具 / MCP / 插件 / 外观 / 界面 / 快捷键 / 引擎 / 关于；插件入口回归设置页，更新检查已收敛
- **长任务感知性**（来自开发版预研）：`task-progress` 事件 + 计划卡进度 + 无进展停滞兜底（手动 继续 / 中止，工具在跑不误判）
- **引擎对齐**：系统提示词改为单助手（无编队段、无角色路由、无技能注入、无工作流模板）；默认工具集覆盖全部内置工具（文件 / 命令 / 终端 / 网络 / 浏览器 / 桌面 / MCP / 插件）并保留风险确认与权限分级
- **工程纪律**：`tsc` 0 报错（renderer + electron 双 tsconfig）、lint 0 错误、单测同步对齐

## v0.4.3（2026-08-23）技能生态

- 技能编辑器与校验：4 规则校验（name/description/triggers 必填、description ≤ 100、正文含 `## 步骤`、triggers 正则合法；未知 tools 仅 warn），写盘路径白名单防穿越；新增 `skills:validate` / `skills:write` / `skills:stats`
- 命中统计：按日聚合的 `skill_stats`（hit / trigger / ok），设置页展示近 30 天命中率 / 触发率 / 成功率
- 模板库 5 → 10：新增 json-validator / log-troubleshoot / csv-clean / api-doc-gen / backup-script 五个内置技能
- 注入预算监控：命中技能超 2 个按相关度截断并记录 `skill.budget-truncate` trace
- 更名与品牌：应用更名为 Acheron-Agent（窗口 / 标题 / 安装包产物名），黄泉人设插画用于欢迎 / 侧栏 / 关于 / 角标；去除第三方品牌词
- 界面：输入区下拉点外部自动收回；若干渲染稳定性修复
- 性能与工程：Vite 分包优化（主 bundle ~1.14MB → 346KB）；`noUnusedLocals / noUnusedParameters` 全开，`tsc` 0 报错；移除多余依赖（含 `concurrently`）；全局错误捕获 + 渲染崩溃自动重建
- 336 项测试全绿

## v0.4.2（2026-08-21）UI 重构与稳定性加固

- 界面重构：标题栏 / 侧栏 / 状态栏 / 设置页 / 路由浮层全面重排，输出统一为流式 Markdown 渲染，审批卡内联展示
- 稳定性：渲染进程崩溃后重建窗口自动恢复，工具执行超时兜底，错误边界重构，Electron 升至 43.4.1
- 清理与验证：删除冗余组件与死代码

> 更早版本见 Git 历史。

