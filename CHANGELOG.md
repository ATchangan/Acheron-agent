# 更新日志（重点）

## v0.4.2（2026-08-21）UI 重构
- 输出样式严格对齐参考产品（实测 DOM 逐项校准）：
  - 删除顶部「执行计划」卡，改为回合内行内审批卡（待批准 + 步骤编号摘要 + 拒绝/批准执行）
  - 代码块复刻 CodeCard：圆角 10px 编辑器底色、无边框无语言头、悬停右上角复制按钮、JetBrains Mono 11.2px/18.2px；shiki 双主题（github-light-default / github-dark-dimmed）用 light-dark() 落地 token 上色，深浅主题自动适配
  - 正文 13px/19.5px、段落间距 11.2px、标题 16/15/14/13px、列表 16.25px 缩进 21px、行内代码 11.7px rgba(255,255,255,.88)/rgba(255,255,255,.07)
  - 用户气泡对齐 composer-human-message：#1a1f27、圆角 3.2px、内边距 8px 36px 8px 12px、细蓝边 rgba(123,179,243,.145)
  - 思考过程对齐 ThinkingDisclosure：11px 状态行 + 整体 0.67 透明度（悬停恢复 1）、正文与回复同字号
  - 回合间距固定 6px、助手消息左缩进 12px；建议 pills 仅空会话显示；底部时间/时长/token 元数据悬停浮现
  - 移除主区顶部会话 tab 栏（会话切换统一走侧边栏）
  - 修复本地服务随机端口导致 localStorage 跨重启失效（引导跳过/消息回应/侧栏顺序/快捷键等记不住）：固定 2101 端口、占用则顺延
  - 冗余清理：删除无引用的 FilesView 与 store/model-pick、parse-utils、reliability、router、project-ctx、constants（含配套测试）；移除 40+ 条无引用的 legacy CSS（markdown-body / message-* / thinking-* / tool-chip / task-* / modal / stat / work-step / voice-btn 等）；移除未使用的 react-markdown、remark-gfm、rehype-highlight、rehype-katex、remark-math、ws 依赖；修正重复的 TTS 开关（策略页重复项删除）；数学公式渲染设置改为真实生效（仅 KaTeX/不渲染，StreamMarkdown 按设置启停 math 插件）
  - 品牌词清理：全仓移除参考产品与构建工具的品牌词（注释、文档、组件命名）；Markdown 渲染组件改名 StreamMarkdown；参考产品同款主题改名为中性的「深蓝」（id: ocean）；品牌扫描脚本改为纯 node 实现（原 rg 依赖在本环境失效）并支持独立词检测，发布门禁可用
  - 修复「补充更多上下文」展开面板格式：新增标题行（补充上下文 / 随消息一起发送），输入框改为与主输入一致的字号、主色与半透明底，去掉突兀的深色小方框
  - 修复回复下方「+」（添加回应）菜单格式：消息靠近顶部时菜单不再向上弹出被标题栏/滚动容器裁切，自动改为向下弹出；层级提升到 300 保证可见
  - 去 emoji：替换界面中唯一的 emoji 风格符号「⏱」（消息底部回合时长），改为文字「耗时」；全仓扫描确认 src 无 emoji 字符残留
  - 工具调用修复：工具行实时执行出错时立即显示错误态（不再误显示“运行中”），错误详情直接展示；工具参数展开限制 4000 字符防止超大 base64 撑爆界面；补全 60+ 个工具的中文名映射（如 apply_patch/Git/MCP/终端/浏览器等），工具行名称不再显示英文原名
  - 稳定性加固：
    - 工具执行加超时兜底：命令类(exec_command/terminal_run)由停止按钮控制不设限，媒体生成 15 分钟，其余工具 10 分钟，超时自动中止并返回错误，任务不再因单个工具挂起而卡死；工具异常转 E: 字符串，不抛穿任务循环；工具正常返回时清理计时器避免定时器泄漏
    - Markdown 渲染加双层保护：内容超过 20 万字符自动降级纯文本；Streamdown 解析/渲染异常由局部错误边界降级为纯文本，单条坏消息不再炸掉整个渲染进程
    - 错误边界重构：新增可复用 ErrorBoundary（错误页带重新加载按钮）；聊天区局部边界——聊天渲染崩溃只替换聊天内容区，标题栏/侧栏/设置等外壳保持可用；window 运行时错误页也带重新加载按钮
- 外壳重构： 34px 标题栏（左工具簇：侧栏开关 / 面板翻转 / 新对话；右工具簇：上下文用量 / 命令面板 / 右栏开关 / 设置），会话标题与模式徽标常驻
- 侧栏优化：会话按「置顶 / 今天 / 昨天 / 更早」日期分组，分区可折叠；新对话按钮；无边框搜索框；行内悬停置顶 / 删除
- 右栏面板：文件树 + 预览双 tab，可拖拽调宽，标题栏一键显隐；「文件」导航改为打开右栏
- 状态栏优化：左簇（模式 / 模型 / 会话 / 工作目录 / 自定义状态行）+ 右簇（版本 / 命令面板快捷键）；上下文用量浮层展示已用 / 上限 / 占比 / 输入 / 输出
- 命令面板：Ctrl+K 唤起，键盘导航，支持跳转视图 / 新对话 / 模式切换 / 主题切换 / 栏显隐
- 路由浮层：设置 / 角色编队 / 记忆改为  Overlay 卡片（Esc 或关闭按钮返回），聊天保持底层
- 会话输出优化：时间线时间戳（HH:MM 同分钟去重）、思考过程状态标签（思考中+计时 → 思考了 Xs / 快速思考 / 思考）、回合时长徽标、流式停滞提示（2 秒无新 token 显示等待计时）、助手操作栏底部右对齐悬停浮现
- 设置页优化：OverlayView 去标题栏改拖拽条 + 悬浮关闭按钮（浅色毛玻璃遮罩 + rounded-xl 卡片）；设置改为 13rem 左导航分组（组间留白、28px 行、图标+标签）+ 右主内容区，导入/导出/重置移至导航底部
- 子代理页优化：编队管理改为运行时子代理活动面板（对齐参考实现）——默认单 Agent 直接执行，无分派时显示空状态；有 dispatch/任务时展示摘要（任务/进行中/失败/步数）+ 进行中/最近完成分组 + 状态行（目标/角色·耗时·步数·更新，可展开步骤流）；角色配置保留在 设置 → 协作
- 版块补齐（对齐参考产品）：
  - 独立页面：技能、产物（工作区页面）；定时任务、命令中心（健康卡片 + 快捷维护）、配置档案（设置快照，创建/切换/重命名/删除）为 Overlay；侧栏常驻页面导航行，⌘K 命令面板同步扩充
  - 会话管理：归档/恢复（新增无损 setArchived IPC，不触碰消息历史）、归档筛选（带计数角标）、按更新时间/Token 用量排序
  - Composer：建议 pills（空输入快捷建议）、模型选择改为  pill + 分组菜单，移除单 Agent 模式下的角色下拉
  - 消息交互：文字标签回应（赞/心/妙/疑惑/鼓掌，本地持久化）、链接预览卡
  - 状态栏：右键自定义显示项（8 项开关 + 恢复默认 + 隐藏状态栏，本地持久化）
  - API Keys 管理页：服务商密钥脱敏展示/显示/复制/删除 + 新增服务商表单
  - 快捷键系统：可重绑（命令面板/新对话/侧栏/右栏/设置，默认 Ctrl+K/N/B/J/,），设置 → 快捷键 录制与恢复默认；标题栏提示同步显示当前绑定
  - 右栏面板：新增终端（工作目录执行命令、滚动日志）与评审 tab
  - 系统通知：任务完成/失败/中止弹桌面通知，受 设置 → 引擎 → 桌面通知 开关控制（主进程接线，测试环境安全降级）
  - 语言收敛：仅简体中文 —— 移除 设置 → 角色 的多语言回复选项（繁体/英文/日文/自动/跟随提问），引擎系统提示强制简体中文回复；打包 locale 只保留 zh-CN
  - 代码块语法高亮：接入 rehype-highlight（highlight.js github-dark），助手回复与思考过程里的围栏代码自动高亮，深浅主题自适应
  - 主题画廊：新增 6 套预设主题（深蓝/墨绿/琥珀/粉彩/石墨/极光），外观页共 12 套主题卡片，一键切换
  - 分栏多会话：标题栏新增分栏工具，选择任一历史会话在主区右侧并排只读展示（直接从磁盘加载消息，支持 Markdown/思考/工具行），可一键关闭
  - 配色重构：深色为深蓝底(#0D2667/#09286F) + 奶油暖字(#FFE6CB) + 蓝强调(#0053FD)，浅色为冷白底 + 蓝强调；语义色(红/绿/蓝/紫)与边框/悬停/选中全部按统一令牌体系换算；默认主题(violet/dark/black)统一为深色，按钮白字改为主题反色
  - 界面修复：补定义缺失的 --bg-hover-strong（激活/强悬停背景），侧栏页面导航按钮重置系统默认灰色底与白边框（扁平导航行），默认主题持久化为 深色
  - 主题体系：内置 ocean/midnight/ember/mono/cyberpunk/slate 六套预设（深蓝底 #0D2F86 + 奶油字 #FFE6CB + 蓝 #0053FD 等）；外观页只显示内置预设 + 浅色 + 自定义；品牌 logo 为白色瓷片 + 深色字标
  - 默认主题调整：默认改为「极黑」（中性黑 #0E0E0E + 灰白字），新增「跟随系统」（auto，深浅色实时跟随系统）与「黄泉」（品牌紫 #121014 + 紫强调 #A78BFA）预设；外观页顺序：跟随系统 / 极黑 / 黄泉 / 内置预设 / 浅色 / 自定义
  - 版块继续补齐：
    - 会话管理：拖拽排序（本地持久化）、只看进行中筛选、双行卡片行（标题 + 时间/Token 元信息）
    - 消息：用户气泡「从此处新开分支会话」（复制该条之前的消息到新会话）、代码块复制按钮、图片点击缩放预览、KaTeX 公式渲染（remark-math + rehype-katex）
    - Composer：@文件引用弹出（工作目录文件，键盘选择）、语音输入（Web Speech 中文）、粘贴到聊天区任意位置自动聚焦输入框
    - 右栏：预览 tab 升级为文件阅读器（文本 / 图片 / PDF，工作目录文件列表）
    - 状态栏：模型目录菜单（文字模型一键切换，复用主模型选择逻辑）
    - 命令面板：新增「设置」分组（供应商/策略/外观/界面/快捷键/记忆/引擎/关于 深链到对应 tab）
    - 分栏：分隔条可拖拽调宽（25%–75%）
  - 版块继续补齐（第二轮）：
    - 会话分页：列表超过 50 条显示「加载更多」（分页加载）；移除语音输入（按用户要求）
    - 侧栏项目区：扫描工作目录下的 git 仓库，显示仓库名与当前分支，点击切换工作目录
    - 评审面板：git status / git diff 一键查看，diff 行按 +/-/@@ 高亮
    - 终端面板：命令历史（↑/↓ 回看）、清空输出按钮
    - 首次引导：三步引导（欢迎 → 配置服务商 → 工作目录 → 完成），完成后不再显示
  - 版块继续补齐（第三轮）：
    - 多 tile 分栏：支持「右侧 / 下方」两种方向，分栏窗格带会话 tab 下拉切换（8+ 会话可选）、分隔条按方向拖拽调宽
    - 任务监视窗：主进程新增 watch:open IPC，独立置顶小窗加载 #watch 路由，2 秒轮询实时查看指定会话；子代理页运行中任务带「监视」按钮
    - 启动更新横幅：启动异步检查更新，发现新版本右下角弹出横幅（下载进度 + 忽略），下载完成后消失
    - 引导调整：首次引导支持「跳过引导，不再显示」；点击任意配置步骤（供应商/工作目录）即视为完成引导，之后重启不再出现
    - 会话 tab 栏：主区顶部显示最近 8 个会话 tab（点击切换、删除、忙碌脉冲、+ 新对话），对齐参考 的 tab 化多 tile
    - 会话输出补齐：流式输出改为边生成边渲染 Markdown（含代码高亮/公式，Streamdown 效果）；回合末尾新增「改动文件卡」（改动了 N 个文件 + 回滚/忽略，从原头部错误条迁入）
    - clarify 交互（ClarifyTool）：引擎新增 clarify 工具（question/choices/multi_select），模型提问时暂停等待用户选择；渲染层回合内显示选项卡片（字母徽标选项、自定义回答、跳过、发送），选择经 engine:clarifyRespond 回传模型继续执行；停止任务时自动跳过
    - 输出渲染对齐参考 代码：接入 @assistant-ui/react-streamdown（StreamdownTextPrimitive 流式模式 + tailBoundedRemend 不完整 Markdown 尾修复）+ @streamdown/math（记忆化 KaTeX，单 $ 行内公式）+ @streamdown/code（shiki 语法高亮，动态加载）；正文 13px/行高 1.5/段落间距 0.7rem、标题缩到聊天尺度（16/15/14/13px）、表格边框卡片、引用/内联代码/图片缩放，流式与完成态共用同一管线
    - 输出结构对齐（实测校准）：正文 13px/行高 19.5px、段落间距 11.2px、列表边距 16.25px/li 6.5px、行内代码白字 11.7px、strong 600、助手缩进 12px、线程横向 24px；代码块交给 streamdown 原生 code-card（shiki + 语言头 + 复制）；顶部「执行计划」卡移除，改为回合内行内审批卡（）
- 体验：侧栏与右栏一键互换（面板翻转）、状态栏可隐藏、Ctrl+N 新对话；移除临时 CDP 调试开关

## v0.4.1（2026-08-15）
- 去品牌化：移除 IP 角色名/人设/主题等品牌词（产品名沿用 Acheron-agent），角色改为「主控/文档/安全/通知/陪伴/设计/开发」，默认助手改为「助手」；旧角色名在会话/记忆/设置中自动别名迁移
- 自写插件：Agent 用 install_plugin 生成插件并热加载（含设置 schema，插件页自动渲染设置卡片并以 ctx.settings 注入），插件工具 schema 注入模型，启用/禁用开关真正生效并落盘
- 插件执行隔离：迁入独立 utilityProcess（挂死强杀、崩溃隔离），文件/命令能力逐条回父进程裁决（L4 拦截 + L2/L3 确认），utilityProcess 不可用时回落同进程 vm
- MCP 持久化：服务器配置连接后自动保存，启动按开关自动连接，本地 stdio 断线自动重连（3 次退避）；「启动等待时间」真正生效
- 界面自定义：侧边栏/聊天区/消息元素逐项显隐 + 信息密度三档 + 自定义 CSS + 状态行模板（${model}/${workDir}/${context}/${tokens}/${agents} 自由组合）
- 聊天改设置：get_settings/set_settings 白名单读写全部安全设置（密钥脱敏、风险开关禁止经对话修改），即时生效并持久化
- 稳定性：设置文件热重载（外部编辑合法即生效、非法保留最后可用）、主题色元数据同步、原子写内容去重
- 修复：MCP 工具名含点/空格/双下划线时调用失败；set_workdir 后 exec_command 工作目录不同步；插件目录路径注入错误；插件启用状态随 SQLite 迁移丢失；vm 同步死循环可冻结应用；插件元数据以主进程权限 require 执行；聊天改设置的 uiDisplay 竞态覆盖
- 验证：57 文件 343 用例全绿；插件宿主 Electron 冒烟（正常/挂死/崩溃/命令拒绝）+ asar 打包场景通过

## v0.4.0（2026-08-13 首发，2026-08-15 定稿）
- 记忆基座：本地 SQLite（node:sqlite 零原生依赖）持久化，强杀不丢；旧 JSON 记忆一次性迁移，建库失败自动降级内存模式
- 检索与分层：FTS5（trigram+BM25）与向量 RRF 双路融合检索；三档衰减（30/60/180 天，置顶永存）；四层金字塔 L0 原始记录→L1 原子事实→L2 场景→L3 核心结论，source_id 逐层溯源
- 事实库：中文启发式三元组抽取、精确去重置信度累计、同主谓冲突旧行自动淘汰；情景时间线 recall_events
- 模型网关：任务类型检测（text/code/vision/long，代码识别覆盖围栏代码块/行首命令/源码路径）+ 统一路由 + 降级链；本地视觉推理服务自动切换（加载→启动→健康检查→分析，失败回云，并发队列防 OOM）
- 上下文：大工具结果 side-channel 存档 + recall_tool_output 取回（会话限 5 次）；四要素状态（目标/已完成/产出物/未决）常驻 system 尾部
- 技能：triggers 正则匹配按需注入（单技能≤800 字、top2、命中统计落库）
- 界面清理：删除文件视图顶部的「处理器 / 内存 / 显卡」实时占用栏及其 2.5s 轮询，文件页只保留工作目录与文件树；后端 sysPerf 接口保留未被界面引用
- 工程：渲染层依赖移出运行时依赖；Electron 32→43、React 19、Vite 7、TS 5.9；electron-updater 增量更新（GitHub Release）；语言包裁剪为 zh-CN/en-US
- 0.4.0 定稿收口：移除桌宠（插件伴身/助手 3D 建模/Unity 原型）与相关素材；记忆收敛为 SQLite 单数据源（上下文注入 / recall_memory / 记忆页统一读写 agent.db，教训/目标/情景并入新表，旧 memory.json、memory-vector.json 仅保留一次性迁移读取）；向量检索加内存缓存与模长预存；会话索引改为按 mtime 增量更新并包事务；启动时的索引重建与每日维护延后到窗口显示后；side-channel/审计加保留期清理；每日维护含 PRAGMA optimize 与周级 VACUUM
- 0.4.0 定稿安全与链路：localVision 命令改无 shell 直接 spawn 并按引号切分，服务进程登记在册、退出统一回收；更新下载改流式写入 + 断点续传 + SHA256 校验；备份白名单补 agent.db 并在打包前做 WAL checkpoint，恢复前关闭数据库句柄；CI 升 Node 22、补评估/品牌词/UI 巡检门禁、Release 上传 blockmap 与 latest.yml 供增量更新
- 验证：49 文件 287 用例全绿；Windows 安装包打包通过

## v0.3.9（2026-08-11）
- 正确性：中文乱码修复、子代理统一执行管道、私有记忆隔离、模型降级链、密钥解密兜底、动态危险命令黑名单、MCP 确认统一
- 效率与结构：engine.ts 1749→1139 行、main.ts 489→306 行、渲染层死代码收敛、Agent 定义单源
- 记忆与技能：容量封顶自动归档、约定类自动置顶、内置技能可隐藏、技能同名用户优先
- 协作与纪律：强制 dispatch、handoff 带上下文、交付附「本次改进点」、工具表演检测、长任务并行新会话+完成通知
- 体验：复制按钮图标化、输出 Markdown 排版优化、全仓去 emoji/品牌词
- 工程：发布门禁 release:check、测试数据隔离、评估历史限容
- 验证：42 文件 248 用例全绿；CDP 全功能 35/35、UI 21/21、端到端 12/12

## v0.3.8（2026-08-09）
- 项目指令生态：AGENTS.md/CLAUDE.md/.agents.md 目录链合并、子目录按需注入、注入安全扫描
- 执行机制：原生 git 工具、事件钩子 Hooks、任务文件回滚、自定义子代理、模型失败自动降级、计划一致性
- PowerShell 7 全场景路由、GBK 解码、危险命令黑名单收窄；性能优化（同任务 61.6s→5.1s）
- 一键环境自检 21 项、用户消息重新生成；测试 239 用例

## v0.3.7（2026-08-09）
- 计划执行与验证闭环：工具调用生成步骤卡、收尾复盘、PLANS.md 落盘
- 技能生态：SKILL.md 清单 + read_skill、apply_patch、会话化终端
- 稳定性加固：任务停止即时落盘、渲染崩溃上下文、CPU 渲染防崩溃

## v0.3.6（2026-08-08）
- 流式渲染链路重构（拆订阅 + memo + Markdown 缓存）、事件 delta 增量传输、广播收窄
- 设置页 16 tab 懒加载（主 bundle -47.5%）、轮询收敛

## v0.3.5（2026-08-07）
- token 优化收官：11 个开关单点回退、LLM 摘要驱动压缩、代码结构清理

## v0.3.4（2026-08-06）
- 会话区回合制重构、浏览器面板内嵌实时画面、思考过程持久化、风险确认卡片

## v0.3.3（2026-08-06）
- 独立内核 AgentEngine：主进程直连 LLM、计划确认门、断点恢复
- 上下文压缩、工具缓存、多会话并发、日志轨迹

## v0.3.2（2026-08-05）
- token 优化系列：工具白名单注入、结果瘦身、图片降级、插话合并

## v0.3.1（2026-08-04）
- 全项目重构、会话系统修复、插话队列、安全补丁

## v0.3.0（2026-08-04）
- 架构重构：多角色编队（dispatch/handoff）、语义记忆 v2、Plan-Execute-Verify、浏览器自动化、插件执行层

## v0.2.x（2026-07-29 ~ 08-02）
- 安装包/主题皮肤/浏览器 web_read/GPU 选项/多 Agent 协作/插件录插件/会话导出等迭代

## v0.1.0（2026-07-29）
- 初始发布：Electron 桌面助手、多 Provider LLM、20 个内置工具、会话管理、三套主题
