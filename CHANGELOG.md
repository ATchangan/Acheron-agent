# 更新日志（重点）

## v0.4.0（2026-08-13）
- 记忆基座：本地 SQLite（node:sqlite 零原生依赖）持久化，强杀不丢；旧 JSON 记忆一次性迁移，建库失败自动降级内存模式
- 检索与分层：FTS5（trigram+BM25）与向量 RRF 双路融合检索；三档衰减（30/60/180 天，置顶永存）；四层金字塔 L0 原始记录→L1 原子事实→L2 场景→L3 核心结论，source_id 逐层溯源
- 事实库：中文启发式三元组抽取、精确去重置信度累计、同主谓冲突旧行自动淘汰；情景时间线 recall_events
- 模型网关：任务类型检测（text/code/vision/long）+ 统一路由 + 降级链；本地视觉推理服务自动切换（加载→启动→健康检查→分析，失败回云，并发队列防 OOM）
- 上下文：大工具结果 side-channel 存档 + recall_tool_output 取回（会话限 5 次）；四要素状态（目标/已完成/产出物/未决）常驻 system 尾部
- 技能：triggers 正则匹配按需注入（单技能≤800 字、top2、命中统计落库）
- 桌宠：式神伴身（透明置顶小窗、5 态动画、气泡提醒、8 角色台词、拖拽位置持久化、设置开关、关闭零残留）
- 桌宠 3D 建模：黄泉正常 / 大招（白发）两套 PMX 建模，three.js r158 + MMDLoader 渲染、ammo 头发物理（wasm 内存态注入 + 补刚体阻尼 + 降重力，微风级轻摆，失败自动降级）；Q 版化（大头 + 短躯干骨骼缩放）；程序化自然待机动画（多层噪声呼吸 / 随机张望 / 随机小动作 / 平滑双眨 / 目光跟随鼠标 / 说话嘴型 / 脚下阴影 / 状态参数平滑过渡）；可切换舞蹈动作（极乐净土 / 彩虹节拍 / Good Time）；主进程光标跟随拖动（按住即跟手，鼠标出窗不丢）；大招红光气场；素材版权本地化（不进开源仓库，缺素材自动回退剪影）
- 主界面：交互会话顶部改为黄泉桌宠控制条（开关 / 形态 / 动作 / 回位），聊天/工作模式切换移到侧边栏会话区顶部与设置→外观
- 桌宠聊天：单击桌宠弹出输入框，回车即可直接对话；消息复用主窗口会话管线（历史 / 人设 / 保存全打通），回复流式显示在桌宠气泡
- 桌宠参照 MateEngine：位置锚定三模式（自由漂浮 / 坐视窗跟随活动窗口 / 坐任务栏），窗口坐立平滑过渡、拖动自动脱离锚定；程序化坐姿（大腿前伸、小腿下垂、微风轻晃，臀部精确压在视窗上沿）；坐视窗/任务栏时命中区域收缩为角色轮廓（透明处点击穿透，不挡标题栏）；戳头气泡反馈、戳身体开聊分区；修复 150% DPI 下高频 setPosition 使透明无边框窗口逐帧膨胀 1px 的问题
- 桌宠设置独立页签（从「外观」拆出），大部分参数自由调节并实时生效：大小 / 透明度 / Q版程度 / 呼吸幅度 / 小动作频率 / 置顶 / 气泡 / 目光跟随 / 头发衣摆物理
- 桌宠动作全量回归与视觉验证：14 个状态（正常/大招待机、三支舞蹈、坐视窗/坐任务栏、拖拽、戳头、思考/工作/完成/出错）逐一截图并交视觉模型复核；修复坐任务栏后窗口被 NaN 坐标缩成 38px 且无法恢复的问题；补站姿戳头缩头反馈，拖拽手臂与漂浮幅度调自然，工作状态摆头放缓，出错状态增加后仰缩肩反应；新增 scripts/pet-action-verify.js 回归脚本
- 桌宠材质与骨骼表现修正：修复 sr.ycl.cool 建模中「X+」内衬与正面完全共面导致的 Z-Fighting（裙摆/衣袖/腰部闪烁、条纹、重影），对共面层启用 polygonOffset 压深；含 alpha 通道的衣料材质启用 alphaTest 裁掉全透明像素，消除半透边缘杂色；透明眼影/脸叠加层关闭深度写入；坐/站锚点切换改为约 0.7s 平滑折叠过渡，肩部跟随手臂联动分摊肘腕旋转，减轻肩关节与腿部连接处的僵硬感
- 桌宠性能与动画可调（v0.4.0 追加）：渲染帧率上限 30~144 FPS / 不限（默认 60，按帧间隔节流整个模拟+渲染循环，高刷新率屏幕下显著降低 CPU/GPU 占用，实测 60 上限≈61、30 上限≈30、不限≈124）；坐/站与状态动画过渡速度 150~1500ms 可调（默认 450，越小越快）；新增实际渲染帧计数与 fps/transition 调试探针
- 界面清理（v0.4.0 追加）：删除文件视图顶部的「处理器 / 内存 / 显卡」实时占用栏及其 2.5s 轮询，文件页只保留工作目录与文件树；后端 sysPerf 接口保留未被界面引用
- 桌宠 VRM 支持（v0.4.0 追加）：Blender 5.2 无头管线（mmd_tools 现代分支 + VRM Addon）把黄泉正常/大招两套 PMX 自动转换为 VRM 1.0，含 VRMC_vrm 人形骨骼与 18 个表情、VRMC_springBone 发丝/裙摆/袖子弹簧骨及碰撞体、贴图降采样；桌宠自动检测 pet/models/vrm/*.vrm 并加载（缺失回退 PMX），设置新增「模型格式」VRM/PMX 切换（VRM 默认，PMX 保留 MMD 舞蹈）；VRM 渲染层解析 humanoid 与 springBone 扩展，映射日文骨骼做待机/坐姿/目光/眨眼嘴型，弹簧链按末梢递增幅度做微风级程序化摆动
- 桌宠 VRM 渲染修正（v0.4.0 追加）：发丝/裙摆/袖子材质从 BLEND 半透明改为 alphaTest 裁剪 + 写深度 + 双面渲染，消除多层透明排序造成的头发「一会儿有、一会儿秃」闪烁；弹簧链按骨骼真实父子深度计算摆幅（末梢约 0.16~0.4rad、频率约 0.9Hz），并补呼吸与手臂微摆，解决 VRM 无 ammo 物理导致的静止感；pet:config 改为每次页面加载/刷新都重发，支持渲染层热迭代
- 桌宠 VRM 丝滑化（v0.4.0 追加）：弹簧骨从正弦噪声改为阻尼弹簧求解器，读取 VRMC_springBone 自带的刚度/重力/空气阻尼参数逐帧积分，叠加缓变风场，刘海等无物理链的头发骨骼自动补轻弹簧；VRM 材质改 MToon 渐变硬切 + BlinnPhong 高光，颜色层次与漫画质感更接近 MateEngine 路线；PMX 舞蹈/大招形态回归通过
- 工程：打包瘦身（app.asar 40MB→15.5MB）；Electron 32→43、React 19、Vite 7、TS 5.9；electron-updater 增量更新（GitHub Release）；桌宠基准窗 180×220 → 200×300，PMX/贴图按桌宠分辨率降采样（原始高清件本地归档）
- 验证：46 文件 273 用例全绿；Windows 安装包打包通过

## v0.3.9（2026-08-11）
- 正确性：中文乱码修复、子代理统一执行管道、私有记忆隔离、模型降级链、密钥解密兜底、动态危险命令黑名单、MCP 确认统一
- 效率与结构：engine.ts 1749→1139 行、main.ts 489→306 行、渲染层死代码收敛、Agent 定义单源
- 记忆与技能：容量封顶自动归档、约定类自动置顶、内置技能可隐藏、技能同名用户优先
- 协作与纪律：强制 dispatch、handoff 带上下文、交付附「本次改进点」、工具表演检测、长任务并行新会话+完成通知
- 体验：复制按钮图标化、输出 Markdown 排版优化、全仓去 emoji/品牌词
- 工程：发布门禁 release:check、测试数据隔离、评估历史限容
- 验证：42 文件 248 用例全绿；CDP 全功能 35/35、UI 21/21、端到端 12/12

## v0.3.8（2026-08-09）
- 项目指令生态：AGENTS.md/CLAUDE.md/.agents.md 目录链合并、子目录按需注入、注入安全扫描
- 执行机制：原生 git 工具、事件钩子 Hooks、任务文件回滚、自定义子代理、模型失败自动降级、计划一致性
- PowerShell 7 全场景路由、GBK 解码、危险命令黑名单收窄；性能优化（同任务 61.6s→5.1s）
- 一键环境自检 21 项、用户消息重新生成；测试 239 用例

## v0.3.7（2026-08-09）
- 计划执行与验证闭环：工具调用生成步骤卡、收尾复盘、PLANS.md 落盘
- 技能生态：SKILL.md 清单 + read_skill、apply_patch、会话化终端
- 稳定性加固：任务停止即时落盘、渲染崩溃上下文、CPU 渲染防崩溃

## v0.3.6（2026-08-08）
- 流式渲染链路重构（拆订阅 + memo + Markdown 缓存）、事件 delta 增量传输、广播收窄
- 设置页 16 tab 懒加载（主 bundle -47.5%）、轮询收敛

## v0.3.5（2026-08-07）
- token 优化收官：11 个开关单点回退、LLM 摘要驱动压缩、代码结构清理

## v0.3.4（2026-08-06）
- 会话区回合制重构、浏览器面板内嵌实时画面、思考过程持久化、风险确认卡片

## v0.3.3（2026-08-06）
- 独立内核 AgentEngine：主进程直连 LLM、计划确认门、断点恢复
- 上下文压缩、工具缓存、多会话并发、日志轨迹

## v0.3.2（2026-08-05）
- token 优化系列：工具白名单注入、结果瘦身、图片降级、插话合并

## v0.3.1（2026-08-04）
- 全项目重构、会话系统修复、插话队列、安全补丁

## v0.3.0（2026-08-04）
- 架构重构：多角色编队（dispatch/handoff）、语义记忆 v2、Plan-Execute-Verify、浏览器自动化、插件执行层

## v0.2.x（2026-07-29 ~ 08-02）
- 安装包/主题皮肤/浏览器 web_read/GPU 选项/多 Agent 协作/式神录插件/会话导出等迭代

## v0.1.0（2026-07-29）
- 初始发布：Electron 桌面助手、多 Provider LLM、20 个内置工具、会话管理、三套主题
