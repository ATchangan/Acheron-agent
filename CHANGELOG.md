# 更新日志

## v0.3.3 多 Agent 审查修复（2026-08-06 追加）
- **子任务 token 用量与预算**：dispatch 子任务的 usage 不再丢弃，按 requestId 去重计入会话累计与「单任务 token 预算」，上下文环同步刷新
- **禁用角色拦截**：dispatch 不再调用已禁用角色，也不会把它们加进活跃名单；`list_agents` 不再列出已禁用角色
- **并发上限生效**：「最多同时活跃角色」真正控制 dispatch 并发，超出部分自动排队分批执行
- **交接开关落实**：
  - 关闭「传递完整上下文」后，被交接角色只看到交接点之后的说明/结果与后续执行，不再携带完整历史
  - 开启「完成后自动交回」时，被交接角色输出最终结果后身份自动回到上一角色（默认开启）
- **角色专属模型**：dispatch 子任务优先使用目标角色配置的模型（如黑天鹅 vision），未配置才继承当前模型
- **断点恢复**：checkpoint 记录交接栈/交接点/交接次数，恢复后防反复交接熔断不再失效
- **健壮性**：子任务流式调用补 rejection 兜底，不再产生未处理异常；看门狗超时时顺手中止在跑命令

## v0.3.3 协作链增强（2026-08-06 追加）
- **所有协作上限改为动态自动调节，直到任务完成**：
  - 单批工具轮次：初始沿用「工具调用上限」（默认 50），批内仍有工具待执行时自动每次 +50 顺延（1 万级保险上限，仅防挂死），模型停止调用工具才结束
  - dispatch 子任务轮次：不再固定 8 或 50，仍在推进时自动顺延；同一工具同一参数累计触发熔断（默认 3 次）才会停止，防止死循环
  - 子任务超时：取消 600s 总上限，改为「活动看门狗」——只要流式数据/工具产出仍在刷新就继续等，连续无进展超过「工具超时 + 30s」才判超时
  - dispatch 整体超时：删除固定 300s 掐断，由各子任务的活动看门狗自行决定结束时机
  - 交接链：达到「最大交接链长度」后自动顺延继续，不再降级或报错；同一角色被反复交接（默认 3 次熔断 + 2 次余量）时判定死循环并引导改用 dispatch
- 设置→协作「最大交接链长度」与 设置→引擎「工具调用上限/工具超时」提示文案同步更新，说明动态顺延行为

## v0.3.3 聊天界面极简终端式 + 软件内风险确认 + 原生窗口按钮（2026-08-06 追加）
- **性能优化**：
  - 流式 Markdown 增量渲染：稳定前缀只解析一次（按块边界切分、未闭合代码围栏自动回退），只有尾部随每个 chunk 重解析，长回复不再越流越卡
  - 贴底自动滚动改直接定位 + rAF 合并，不再堆积 smooth 动画
  - 诊断轨迹改内存缓冲 + 500ms 批量写盘，减少主进程同步 I/O
  - 会话启动只读元数据、点开/切换才懒加载全量消息；会话保存改异步原子写，大会话不阻塞主进程
  - 缓存自动释放：文件读取缓存加总字节上限(32MB)+TTL(10分钟)+按体积淘汰；web_read 缓存加 200 条上限+过期清扫；已加载会话内存保留 当前+最近 2 个、其余释放回 meta（磁盘不删，再点开重读）；Chromium 缓存(Cache/Code Cache/GPU)启动超阈值自动清理，设置→引擎→缓存管理 可关/改阈值/立即清理（默认开启，默认 200MB）
  - 新增 `splitStreamMarkdown` 纯函数 5 例单测（总计 17 文件 100 用例）
- **清理头像配置**：聊天已去头像，设置→外观的「头像」区块、设置→角色的头像预览一并删除；类型字段 `agentAvatar/agentAvatarImage` 与主进程 `avatar.dat` 存取逻辑移除
- **浏览器交互升级**：`browse` 改为可访问性快照（标题/正文 + 带 `@编号` 的可交互元素，如 `@1 [link] 文档 -> url`）；新增 `browser_click` / `browser_type` / `browser_press` / `browser_scroll` 交互工具（agent 可真正操作网页，不再只能读全文/截图）；URL 安全校验（仅 http/https，拦截 file://、javascript:）；浏览器面板新增「↗ 系统浏览器」按钮；实测：打开 example.com → 快照 → 点击 @0 → 跳转 IANA 并汇报 ✅
- **聊天界面极简终端式**：去掉消息头像、发送者名称与气泡，消息改为终端式逐行排版——用户消息带 `❯` 前缀强调色、助手消息 Markdown 通栏、工具结果缩进变暗、思考状态降为灰色行内文字、空状态也去掉大头像
- **风险操作软件内确认**：L2/L3 风险确认不再弹原生 Windows 窗口，改为软件右下角确认卡片（显示操作详情 + 拒绝/允许），勾选「本次任务都批准」后同一任务后续操作不再询问（按 sid+taskId 记录，新任务自动失效）；60 秒无人操作自动拒绝并给出明确错误
- **窗口按钮修复**：放弃自定义 HTML 按钮，改用 Electron 原生 `titleBarOverlay` 窗口控制（由操作系统绘制与命中，最小化/最大化/关闭不再受拖拽区/命中层干扰）
- 验证：build ✅ / vitest ✅

## v0.3.3 最终回复截断根治（2026-08-06 追加）
- **修复：任务做到一半“只回半句话就结束”**（实测：`completion_tokens=800` 却只落 21 个字、工具调用整体丢失）
- 根因：`llm-core.ts` 流解析只认 `delta.tool_calls`，部分 OpenAI 兼容网关把工具调用放在最终 chunk 的 `message.tool_calls` 字段整体下发，被当成无效数据丢弃 → 前面那句步骤说明被误当最终回复提交
- 修复：兼容 `message.tool_calls` / `message.content` 下发形式；`finish_reason` 透传
- 加固：工具调用丢失（`finish_reason=tool_calls` 但 0 个调用）、工具参数非 JSON（截断）、输出被截断（`finish_reason=length`）一律自动重试（最多 5 次），不再把半截响应当最终回复；用户手动停止仍立即退出、不触发重试
- **输出上限按轮次自适应**：根因之二——「帮我制作一个小游戏」被输出上限判成简单聊天只给 800 token，模型想一次 write 完整游戏代码直接被腰斩；现在纯聊天（无工具）保留省钱档 800，工具轮用基础上限 4096，截断重试时自动按 2 的幂放大（最大 32 倍），实测模型成功分块写完 15.9KB 的俄罗斯方块 index.html
- **流式超时**：供应商连接挂起不发数据时任务会永远“执行中”——新增 45s 无数据自动断开 + 240s 总超时，断开后按「响应超时」自动重试，不再无限挂起
- **风险确认框超时**：L2/L3 原生确认框 60 秒无人操作自动拒绝并给出明确错误，任务不再无限挂起等点击（实测此前任务两次卡在确认框上）
- 新增 `llm-core` 单测 5 例（message 形式 / delta 形式 / finish_reason 透传 / stall 判定 / 挂起超时）
- 验证：build ✅ / vitest 17 文件 95 用例全绿 ✅ / 真实任务复跑 ✅（俄罗斯方块完整写出）

## v0.3.3 对话聊天升级（2026-08-06 追加）
- **修复极简样式被旧样式覆盖**：`ui-polish.css` 的旧「消息气泡增强」会盖住极简样式（用户/助手消息重新出现背景圆角），已移除气泡并统一为透明通栏
- **用户/助手消息区分强化**：用户消息整体使用强调色 + 加粗 + 左侧 2px 强调色竖线（强调色 + `❯` 标签语义），助手回复保持普通正文色，一眼可辨
- 用户消息改为右对齐（强调竖线同步移到右侧），助手回复保持左对齐
- **会话置顶（永久保留）**：侧边栏每条会话新增 📌 置顶按钮；置顶会话不受「最大会话数」裁剪、不被空会话自动清理删除、永远排在列表最前；置顶状态随会话文件持久化，重启保留；设置→工具→最大会话数语义统一（0=不限，超出只隐藏最早的非置顶会话，文件仍在可搜索找回）
- **任务栏应用名修复**：主进程增加 `app.setName('黄泉Agent')` + `app.setAppUserModelId('com.huangquan.agent')`（与 electron-builder 的 appId 对齐），Windows 任务栏右键菜单不再显示 "Electron"；package.json 增加 productName；**同时显式固定 userData 路径为 `...\huangquan-agent`**（setName 会改变默认 userData 目录导致配置/会话丢失的回归已修复）
- 「最小化/关闭时缩至系统托盘」默认开启（未设置时视为开启），设置→引擎可关闭
- 清理旧头像残留文件 avatar.dat.bak；诊断轨迹退出前强制 flush（不再丢失最后 ~500ms 轨迹）
- **浏览器增强**：新增 `browser_console(expression)` 在页面执行 JS 并返回结果、`browser_vision(question)` 截图 + 视觉模型问答（走策略页配置的视觉模型队列）；**每任务独立浏览器会话**（sid::taskId 隔离，任务结束自动销毁，互不串页面），浏览器面板实时画面跟随最近使用的任务会话
- **上下文环修复**：输入框旁的上下文用量环此前数据源从未更新（永远显示 0）；现在引擎每次 LLM 请求把真实 `prompt_tokens` 作为 `context` 事件推送，渲染层实时更新「已用 X / 上限 Y」，悬停提示注明为最近一次请求的实际输入
- 推理强度调节图标从输入框左侧工具栏移到右侧模型选择器旁边（菜单改为向右展开，避免溢出）
- 输入框下拉菜单对齐修复：`.dropdown-wrap` 增加通用相对定位（此前右侧推理强度菜单锚到整个输入区，点开跑偏），现在所有图标菜单都紧贴图标上方、左右边缘与图标对齐
- 清理源码中外部品牌注释与品牌化类名（改为 `user-prompt`）；全量体检：100 用例全绿、构建通过、供应商/会话/任务/报错状态正常、界面经视觉复核无错位
- **重大修复：会话保存只生效第一次**——`enqueueSave` 队列时序 bug（`saveQueues.set(id, run())` 里 run() 先同步跑完并把“已完成”的 promise 塞回队列），导致每个会话只有首次保存落盘、后续保存全部被吞：新建聊天会话首次保存是空壳「新对话」，带消息的保存全丢，历史会话重启后消失、无法继续聊。修复后多次保存正确合并串行写盘，实测新聊天会话 19 条消息完整落盘 ✅
- 侧边栏只显示当前模式的历史会话（聊天模式只列聊天会话、工作模式只列工作会话），切换靠顶部「聊天/工作」标签；跨模式点选（如搜索结果）的竞态也已修复（先等 setMode 完成再切换，且切换时清空流式串台残留）
- 消息间距只用 `gap` 单一来源（去掉旧 margin 双倍间隔），用户消息加大留白；操作按钮默认弱化、悬停显示；任务卡/工具 chips 改为透明终端式
- **流式回复 Markdown 实时渲染**：引擎流式文字由纯文本升级为 Markdown（代码块/表格/列表随输出生效），90ms 节流防长文卡顿，末尾带闪烁光标
- **回到底部按钮**：上翻查看历史时新内容到达 → 消息区右下角出现「↓」，点击平滑回到底部，贴底自动隐藏
- **复制最后回复**：消息区右下角悬浮「⧉」，一键复制最后一条完整回复的原文 Markdown；流式中复制当前内容，成功后反馈「✓」
- **错误重试**：任务失败错误条新增「重试」按钮，清空错误态并从最后一条用户消息重新执行，不再需要手动重新发送
- **执行中插话标识**：工作中输入文字时发送按钮显示「插」角标 + 输入框动态提示，回车发送=补充指令插话（行为不变，语义更明确）
- 新增纯函数 `isNearBottom` / `latestAssistantText` + 8 例单测
- 验证：build ✅ / vitest ✅

## v0.3.3 最终回复重复根治（2026-08-06）
- **底层消息模型重构**：任务消息流只允许「用户消息 / 步骤卡 / 最终回复」三种消息——不再创建任何流式占位消息；流式文字走临时通道（streamText，不落消息），工具前的文字随步骤卡落一条，最终回复作为独立消息追加
- 重复在架构上不可能发生：不存在“卡片前占位消息”，也就不存在清空/去重/提示词约束等事后补救
- token 累计改为直接消费引擎按 requestId 去重后的 usage 事件（sessTok 镜像），与消息数量解耦
- 验证：build ✅ / vitest 82 ✅ / CDP 实测消息结构 = 用户 → 步骤卡(含步骤文字) → 工具结果 → 最终回复 ✅

## v0.3.3 回复去重与 token 语义（2026-08-06）
- **步骤卡预答去重**：模型在调用工具前输出长方案、最终回复又重复同一段文字时，长预答/重复内容从步骤卡清除，最终回复只显示一次（dedupeStepText 纯函数 + step-update 事件 + 单测）
- **token 显示语义明确**：输入框旁统计改为“累计 入 X / 出 Y”，悬停说明=每次请求 prompt/completion 之和（含工具轮次与子任务），避免误认为当前上下文大小

## v0.3.3 界面微调（2026-08-06）
- 本会话“输入/输出 token”统计从聊天头右上角移到输入框右侧工具栏（token 用量环旁边），聊天头不再拥挤

## v0.3.3 已知小项收尾（2026-08-06 第三轮）
- **死代码全清**：旧渲染层 runtime.ts / subtask.ts / plugins.ts 已删除，`parseDispatchTasks` 迁至纯模块 parse-utils.ts（测试同步指向）；chat.ts 移除全部死 import
- **长命令可被“停止”打断**：exec 执行时注册会话 sid + 环境变量标记，停止时按“根 PID 递归杀整个进程树”（cmd 外层提前退出也能杀到孙进程）；实测 120 秒 ping 命令在停止后立即 aborted
- 验证：build ✅ / vitest 81 ✅ / CDP 实测长命令中断 aborted ✅

## v0.3.3 深度复查修复（2026-08-06 深夜第二轮）
- **多轮对话记忆修复（重大）**：引擎此前只收到“当前轮”消息，跨轮历史丢失；现在渲染层把完整会话历史传入引擎，实测两轮问答（记住暗号→问暗号）正确回答
- **人设修复**：引擎启动时读取 ishiki.md，角色人格不再丢失
- **插话显示修复**：插话气泡只显示用户原始内容，内部“（插话补充…）”前缀只进模型上下文，不再污染界面
- **聊天头部 token 统计修复**：usage 回挂到 assistant 消息，头部“输入/输出”恢复实时统计（实测 5235 token 显示）
- **未配置供应商时忙状态卡死修复**：provider 缺失立即释放 busy/streaming
- **删除运行中会话自动停止引擎任务**，不再后台空转
- **子任务工具执行上下文修复**：dispatch 子任务实际执行工具时也用子角色权限（此前仅工具列表用了子角色）
- **断点恢复供应商修复**：checkpoint 记录当前供应商，恢复时不会把模型挂到错误供应商
- **MCP 开关修复**：mcpAutoInject=false 时全权限角色也不再注入 MCP 工具；新任务启动强制刷新 MCP 清单，刚连接的服务器立即生效
- **计划确认门多会话修复**：planPending 改为按会话存储，多会话并发互不覆盖
- **插话 _inject 标记清理**：任务结束清除标记，历史插话不再被后续请求持续重排
- **细节**：web_read 异常返回原始错误详情；引擎 trace 开关 5s 缓存避免每次事件读设置文件
- 验证：build ✅ / vitest 81 ✅ / CDP 实测 UI 全链路（消息上屏+完成+token 头）+ 多轮记忆 + 插话原始内容 + 停止无重试 ✅

## v0.3.3 修复与打磨（2026-08-06 深夜）
- **停止后不再空响应重试**：任务中止时不再被误判为“空响应”，立即退出并标记 aborted（实测停止后 0 次重试）
- **token 统计去重**：同一请求多次返回 usage 只累计一次，会话/模型统计不再虚高
- **dispatch 子任务工具白名单修复**：子任务改用子角色上下文构建工具列表，父角色白名单不再误伤（三月七→螺丝咕姆代码任务可正常用 read/exec_command）
- **视觉兜底修复**：无视觉模型时不再拿纯文本模型硬调视觉接口，直接提示配置
- **多模型策略回归**：引擎恢复旧 pickModels 调度——简单任务小/快模型、复杂任务大模型、代码/文档任务按需切换
- **MCP 工具默认 ask**：自动注入的 MCP 工具首次调用默认弹确认，可逐工具改为允许/禁止
- **L2 风险确认降噪**：只对会改变系统状态的命令弹确认（node -v/npm ls/git status 等只读查询不再打扰）
- **set_workdir / set_theme 界面联动**：引擎改动通过 ui 事件同步到设置界面并持久化
- **断点瘦身**：checkpoint 剥离图片 dataURL，tasks.json 不再膨胀
- **LLM 摘要压缩（实验）**：llmSummary 开启后长会话早期消息由模型压缩（200 字要点），替代规则截断
- **删除旧渲染层死代码**：runSend/chat-round/chat-llm/resume-ops 已移除，主循环唯一实现在主进程引擎
- **引擎测试**：新增 context/reliability 引擎单测 7 例，总计 81 例全绿

## v0.3.3 独立内核（2026-08-06 深夜）
- **Agent 主循环迁入主进程 `AgentEngine`**：LLM 直连（不经渲染层中转）、工具经 handler 注册表直接分发（复用全部既有实现与安全检查）、上下文构建/记忆/模型调度/视觉队列/子任务 dispatch 全部在主进程执行
- **渲染层变为纯客户端**：只发 start / stop / interject / approve / resume，通过 `engine:event` 事件流展示（assistant 流式 / 步骤卡片 / 工具结果 / final / usage / plan）
- **断点恢复**：工具轮每 5 轮或 30s 将执行现场落盘到任务队列，应用重启后可从断点继续
- **计划确认门（实验）**：设置→引擎→任务可靠性可开启，首次调用工具前展示执行计划，批准后才动手（Claude/Cursor 风格）
- 插话/停止/重发/改向熔断/并行工具/多模型切换/空响应重试/token 预算/诊断轨迹全部由引擎接管
- 验证：`npm run build` ✅ / vitest 74 用例 ✅ / CDP 实测：纯对话任务 + 真实工具调用（ls）均主进程完成、事件流正确、零报错 ✅

## v0.3.3 内核加固：可靠性 / 存储 / 安全 / 可观测性（2026-08-06）
- **任务可靠性**：空响应自动重试至少 5 次改为指数退避（带抖动）；新增单任务 token 预算（设置→引擎→任务可靠性，0=不限），超限提前结束防止失控花费；持久化任务队列落盘（tasks.json），应用异常退出后聊天页顶部显示中断任务并一键恢复
- **MCP 工具自动注入**：连接过的 MCP 服务器 tools/list schema 自动并入 LLM 工具列表（`mcp__服务器__工具` 命名），无需手动 mcp_call；工具权限表支持对单个 MCP 工具 deny/ask，ask 时主进程弹原生确认框；设置→引擎可整体关闭
- **存储加固**：会话/设置/记忆/任务队列全部改为原子写（临时文件+覆盖），崩溃不再产生半写 JSON；会话搜索升级为 FTS5-lite 倒排索引（英文单词+中文 bigram，增量更新），替代全量 JSON 扫描，历史会话检索更快
- **安全闭环**：L2/L3 风险操作（非只读命令执行、删除文件、系统路径写入）默认弹原生确认框（设置可关）；主窗口开启渲染沙箱（sandbox:true）；页面增加 CSP（禁 object/iframe，脚本仅 self+unsafe-eval）
- **可观测性**：本地诊断轨迹 agent-trace.jsonl——按任务/LLM 轮次/工具调用/错误/预算记录完整调用链（requestId 串联），设置页新增「诊断」Tab 可过滤/查看/清空，不上传任何数据
- 工具重试/LLM 空响应重试统一走指数退避；测试新增 reliability 4 例，总计 74 例全绿
- 修复：应用启动自动创建工作台目录误触发 L2 风险确认弹窗（改走专用 mkdir IPC，不再弹窗）

## v0.3.3 界面调整：工作步骤卡片（2026-08-06）
- **移除聊天框右侧终端面板**：工具日志不再常驻右侧；文件树/工作目录/系统信息集成到左侧导航新增的「文件」视图
- **工作步骤卡片**：AI 工作过程改为聊天流内的自然语言步骤卡片（模型每次调用工具前生成一句步骤说明），工具以 chips 显示实时状态（进行中/完成/失败），点击可展开参数与完整结果
- 提示词同步更新：工作模式要求“调用工具前先用一句话说明这一步在做什么”，工具执行期间不再输出其他文字
- 样式细化：步骤卡片改为深色列表式——头部显示“思考了 Xs”，每行工具显示 状态图标 + 中文名 + 参数摘要 + 耗时，底部汇总“共 N 个工具 · 总耗时 Xs”
- 任务级合并：同一任务（用户消息之间）的多张步骤卡片合并为一张「任务进程卡」（N步 · M工具 · 总耗时），最终回复弹出为独立消息
- 默认折叠：任务执行中自动展开看过程；有错误自动展开；正常完成后自动收起成一行；点击头部可手动展开/收起
- 思考气泡退役：任务一旦产生步骤卡片，独立“思考中”气泡不再闪现，状态由任务进程卡头部承担（执行中/思考中/有错误/已完成）

## v0.3.2（2026-08-06 发布）
- 原 0.3.2~0.3.5 token 优化系列合并为一个发布版本 **v0.3.2**（当前版本号）
- 本版包含：token 优化①注入层/②上下文层/③度量层/④可控层、主流桌面 Agent 优点吸收、关于页动态版本号、模型读取增强、界面中文化与去 AI 味、移除符文工坊
- 下方 0.3.1~0.3.5 条目保留为系列开发历史

## 主流桌面 Agent 优点吸收（2026-08-05，0.3.1~0.3.5 全版本）
- **记忆安全扫描**：`scanMemoryText` 检测凭证/API Key/提示注入，save_memory/自动摘要写入前拦截
- **记忆冻结快照 + 使用率**：任务开始冻结一次记忆，会话内各轮一致；记忆块头部显示容量
- **AGENTS.md 项目指令**：工作目录约定自动注入 system 尾部（`project-ctx` 模块 + `get:projectContext` IPC）
- **会话搜索工具**（轻量版）：`session_search` 关键词检索历史会话（主进程 `sessions:search`）
- **工具调用回复去重**：DeepSeek 等模型工具调用前预答与最终回复相同则丢弃重复段
- 各版本 build/测试全绿，启动 + session_search 实测通过；方案库 0.4.0/0.4.1 已标注衔接

## v0.3.2 发布前修复（2026-08-05）
- **关于页版本号动态化**：修复 0.3.2~0.3.5 关于页硬编码显示 v0.3.1 的问题
  - 新增 `app:info` IPC（主进程 `app.getVersion()` + `process.versions`）
  - preload 暴露 `appInfo`，AboutTab 动态显示 版本/Electron/React/Node
  - 同步修复 Node 版本硬编码 22.x（实际 Electron 32 内置 Node 20.18.1）
- 新增版本验证工具：`scripts/verify-cdp.cjs`（CDP 页面健康检查 + 截图 + 关于页校验）+ `scripts/launch-verify.ps1`

## v0.3.5（2026-08-04）

### Token 优化 ④ 可控层（系列收官）
- **T1 并行结果总量护栏**：单轮并行结果 >6000 字符且 >4 个时，保留前 4 个全量、其余走共享瘦身函数（terminal 面板仍保留完整版）
- **T2 优化开关集中（11 个）**：设置→高级→性能优化（Token）区块，0.3.2~0.3.5 全部优化默认全开、单点可关闭即时回退并持久化；含"恢复默认"与开启计数
- **T3 全系列基准验证**：`scripts/token-baseline.mjs` 扩展为 0.3.0 vs 0.3.5 九任务对比表（token/请求数/工具次数/完成度/输出字数），失败任务二分定位
- **T4 文档化**：README 新增 Token 优化章节（优化项清单/关键参数/缓存友好约定/基准门禁）
- 测试新增 4 例（slimToolResult/开关接线/开关迁移），总计 52 例全绿

> 系列总目标：闲聊单请求省 70%+、复杂任务省 50%+（以 token-baseline-report.md 实测为准）；智力门禁 6 项全过才可发布

## v0.3.4（2026-08-04）

### Token 优化 ③ 度量层
- **T1 分层 token 估算 + 实测校准**：代码块/中文/URL/剩余四路分层估算；每次响应用 usage 实测按模型 EMA 更新校准系数（限幅 0.3~3，防单次异常拉偏）
- **T2 缓存策略显式化**：新增 `scripts/check-prefix-stable.mjs` 前缀稳定性断言（去除记忆/归档/workflows/时间戳动态段后对比）；设置页上下文管理区增加缓存友好度说明
- **T3 多段插话合并**：同一轮内多段文本插话合并为单条编号补充指令（全量保留；超长 >1500 保末段独立；带图插话不合并）
- **T4 压缩阈值基准机制**：新增 `scripts/token-baseline.mjs` 9 任务基准集（与基线报告同套），三档对比（0.60/0.70/0.80）数据驱动调参；默认值维持 0.7
- 测试新增 6 例（分层估算/EMA 校准/插话合并），总计 48 例全绿

## v0.3.3（2026-08-04）

### Token 优化 ② 上下文层
- **T1 历史图片降级**：同图只保留"用户最新消息"中的原图，更早轮次的图降级为一行文字提示（视觉会话 token 降 60%+；无图任务请求体零变化）
- **T2 历史 tool_calls 参数截断**：定位类字段（path/query/url 等 25 个）全量保留，超长内容字段截断至 200 字 + 省略标记；字符串数组超 20 项截断；嵌套对象不截（保结构合法）
- **T3 跨任务归档**：任务块（以 user 消息为界）满足"≥6 消息 + ≥2 次工具调用 + ≥2 块"时，最早任务块整体折叠为归档记录（目标/结论/产出物路径/工具聚合），注入 system 尾部；`taskArchive` 开关默认开启可关闭
- 测试新增 4 例（归档边界/图片降级/参数截断），总计 42 例全绿

## v0.3.2（2026-08-04）

### Token 优化 ① 注入层
- **T1 工具白名单注入**：主请求按当前会话 Agent 白名单裁剪工具 schema（知更鸟闲聊 ~6000 token → ~800；子任务与主请求统一过滤函数源；插件工具恒保留）
- **T2 工具 schema 中文精简**：40+ 工具描述中文化、删冗余长句，全量 schema 体积显著下降（参数名/类型/enum/required/安全限制保留）
- **T3 工具结果瘦身提前触发**：>1500 字符即截断（原 3000），头尾 800/500 + 关键行保留
- **T4 记忆按相关度裁剪**：长期记忆按与最近消息的关键词重合度取 top5、情景摘要 3→2、总量护栏 2500 字符；记忆段移出 system prompt 头部（前缀缓存友好）
- **T5 workflows 按需注入**：命中触发词才注入完整模板列表，未命中仅一行引导（每请求省 ~200 token）
- **T6 历史工具轮次折叠**：超过 8 对完整轮次时，最旧 4 对折叠为一条归档摘要（配对不完整自动跳过，防 API 非法序列）
- **T7 输出上限分级**：短闲聊消息 max_tokens 降为 800，代码/文件/任务类保持全局上限（杜绝截断）
- **T8 会话 token 统计**：聊天头部显示本会话累计输入/输出 token（从消息 usage 重算，不新增存储）
- **T9 工具名单瘦身**：system prompt 不再冗余注入工具名列表，改为一行"可用工具范围"
- **T10 静态区块瘦身**：任务闭环/下载/最终回复格式段压缩冗余表述，约束字段逐字保留（每请求省 200~400 token）
- 测试新增 9 例（outputLimit/sessionTokens/foldToolRounds），总计 38 例全绿

> 基线：docs/baseline-intelligence-report.md（智力对照模板，需真实模型跑数后填写）

## v0.3.1（2026-08-05）

### 会话修复（块 A~F）
- 会话级并发状态模块 `session-state.ts`：取代全局 `window.__huangquan_agent` / 全局 streaming / 全局 taskGen
- 停止/重发/自动续跑：终止仅作用于当前会话（会话级任务代号 + abort 带会话过滤）
- send 幂等去重（同一内容 500ms 内重复发送忽略）
- 清空边界 / 中途保存（长任务每 30 秒自动落盘）
- 主进程保存队列：防抖合并、meta 写盘绑定、load 失败标记
- 多会话并发互不串台（Agent 状态/插话队列/阶段气泡全部会话级）

### 重构（块 G~N）
- **主进程拆分**：main.ts 108KB → 28KB，107 个 IPC handler 全部迁入 `electron/ipc/` 18 个域文件（行为零变化、通道名零变化）
- **SettingsView 拆分**：165KB → 11.9KB 壳，13 个 tab 迁入 `src/components/settings/`
- **chat.ts 拆分**：51KB → 10.4KB，send 主逻辑迁入 `chat-send.ts`
- **补丁注释清零**：94 处 `v0.x.x-fix` 前缀清理
- **组件层整理**：全部组件 ≤25KB（PlanningView/CodeView/PluginsView 拆分）
- **类型体系**：全库 any 清零（0 处）
- **测试基座**：vitest 引入，3 测试文件 10 用例

### v0.3.1 补丁（安全加固 + 构建门禁）
- **workflow 工具防挂起**：脚本未调用 `ctx.done` 或返回 Promise 后不 resolve 时，30 秒超时兜底 + 普通返回值自动收尾（原实现会永久卡住工具循环，stop 也救不回）
- **插件沙箱 fs 路径限制**：`require('fs')` 白名单全部包一层工作目录校验，插件不再能读取工作目录外的任意文件（与弹窗文案「仅限工作目录」一致）
- **构建类型门禁**：`npm run build` 增加渲染层 `tsc --noEmit`（原来只查 electron 端，渲染层类型错误不会让 CI 失败）
- **LLM 日志脱敏**：失败日志不再输出用户消息内容（只保留 role/工具结构）；`[LLM]` 调试日志收敛到 `HQ_LLM_DEBUG` 环境变量
- 插话队列补丁 M1~M4（有界合并/改向熔断/发送锁/序列断言）此前已并入本版

## v0.3.0（2026-07）
- 插件沙箱 / 供应商统一模板 / 视觉队列 / 多 Agent 协作修复 / 45 项全量回归
